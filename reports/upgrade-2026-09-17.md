# Sunday Upgrade Report — 2026-09-17 (on-demand run, prod account)

Session: Remote Control, `ijefferson.admin` (prod) account on marvin, branch
`chore/upgrade-2026-09-17`, started 18:25 EDT / 22:25 UTC, this report written
~18:50 EDT / 22:50 UTC (~25 min elapsed, well inside the 90-minute budget).

Today (2026-09-17) is a **Thursday**, not the Sunday 03:00/06:00 ET launchd
cadence — this is an on-demand run (multiple `tmux` sessions in
`reports/upgrade-run-2026-09-17.log` show earlier same-day attempts that
failed to submit their mission due to a `--allowedTools` argv-parsing bug in
`scripts/sunday-upgrade.sh`, fixed in this session's pre-existing uncommitted
working-tree change — see §6).

## 1. Summary line

**PARTIAL (prod promotion BLOCKED-awaiting-dev; read-only inventory/scan/CVE-triage phases completed and one real fix shipped)**

Nothing was applied to prod's running stack. Per the mission's cross-account
contract, prod may promote only the exact version set dev already passed —
and dev has not published a passing result for today.

## 2. Environment mapping (discovered, not assumed)

| Concept | Evidence |
|---|---|
| This session's account | `ijefferson.admin` (prod), host `marvin.local` |
| Dev account | `agentshroud-bot`, separate Colima VM on the same physical machine — this session cannot start/stop/exec into it |
| Dev→prod handoff | `/Users/Shared/agentshroud-sunday/dev-result-<date>.json`, written only by `scripts/sunday-upgrade-apply.sh --env dev` |
| Prod compose stack | `docker/docker-compose.yml`, project `agentshroud`, profile `full` — services: `gateway`, `openclaw`, `hermes`, `wazuh-agent`, `docker-socket-proxy`, `voice-gateway` |
| Deterministic script | `scripts/sunday-upgrade-apply.sh --env dev\|prod --phase ...` — the only thing authorized to mutate `docker/versions.env` or rebuild/redeploy |
| Version source of truth | `docker/versions.env` (OpenClaw + Hermes), `gateway/__init__.py` mirrored there (AgentShroud's own version) |

## 3. Dev handoff check (gating decision)

```
$ ls -la /Users/Shared/agentshroud-sunday/ | grep 09-17
(no output — file does not exist)

$ cat /Users/Shared/agentshroud-sunday/dev-result-2026-09-15.json
{
  "date": "2026-09-15",
  "status": "FAIL",
  "phase": "all",
  "upgraded": false,
  "latest_openclaw_seen": "2026.9.4",
  "versions": {
    "AGENTSHROUD_VERSION": "1.6.1",
    "OPENCLAW_VERSION": "2026.7.1-2",
    "HERMES_IMAGE": "nousresearch/hermes-agent@sha256:68e15ae2...",
    "HERMES_VERSION": "0.20.1"
  }
}
```

No `dev-result-2026-09-17.json` exists; the most recent handoff (2026-09-15)
is `status: FAIL`. Per the mission's ground rule ("if missing or status !=
PASS, apply NOTHING to prod"), **every prod version bump this run is
BLOCKED-awaiting-dev.** I did not attempt to run the dev job myself — this
session physically cannot reach the `agentshroud-bot` account/VM.

## 4. Preflight (prod, read-only)

```
$ bash scripts/sunday-upgrade-apply.sh --env prod --phase preflight
...
[apply] preflight: docker daemon reachable
[apply] preflight: compose config valid
[apply] preflight: host volume 155GiB free
ERROR: preflight: Docker storage has only 16GiB free — image builds need >=25GiB.
exit code: 10
```
Confirmed independently:
```
$ colima ssh -- df -h /var/lib/docker
/dev/vdb1   157G  134G   16G  90% /var/lib/docker
$ docker system df
Images         130GB   35.34GB reclaimable
Build Cache    66.69GB 11.83GB reclaimable
```
This is real and would block an `apply` phase even if dev had passed today —
moot for this run since prod isn't applying anything, but it is a genuine
blocker for **next** time. Not remediated in this run (no images pruned):
disk cleanup wasn't required to complete the read-only phases, and pruning
non-rollback-tagged images without the context of *why* each was kept felt
like unauthorized scope creep for a run that is otherwise strictly
read-only. **Recommendation for the next run/human**: `docker builder prune
-f` (11.83GB, zero risk — build cache only) at minimum; consider `docker
image prune -a --filter 'until=168h'` for the rest, excluding
`agentshroud-rollback/*` tags newer than 7 days.

## 5. Inventory — wrapped agents (mandatory script, cited)

```
$ python3 scripts/discover_upstream_versions.py --json
{
  "openclaw": {"latest": "2026.9.4", "source": "npm:openclaw", "error": null},
  "hermes":   {"latest": "v2026.9.14", "digest": "sha256:99641e57...",
               "source": "dockerhub:nousresearch/hermes-agent", "error": null}
}
```

| Component | Pinned (docker/versions.env) | Latest stable | Status | Source |
|---|---|---|---|---|
| OpenClaw | `2026.9.4` | `2026.9.4` | **ALREADY CURRENT** | npm registry (`openclaw` pkg) — verified live above. Bumped 2026-09-16 in commit `0770766bba` (already on `origin/main`, pre-dates this run). |
| Hermes | image digest `sha256:68e15ae2...` (labeled `HERMES_VERSION=0.20.1`) | tag `v2026.9.14`, digest `sha256:99641e57...` | **BEHIND — BLOCKED-awaiting-dev** | Docker Hub tags, verified live above. Digest differs from what's pinned, so a newer image genuinely exists. |

Hermes has a repeated history of being blocked on this exact upgrade: the
2026-08-30 dev-result recorded `"hermes": "BLOCKED (sha256:20c5941...
fails A2A probe, both sides agreed)"`. Whether `v2026.9.14` clears that
probe is unverified this run — that compatibility check runs as part of
dev's `apply`/`verify` phases, which this account cannot execute. **This is
exactly the kind of decision dev's `--phase all` run is supposed to make and
publish; today it hasn't.**

## 6. Preflight/hygiene (git)

- Working tree had one pre-existing uncommitted file at session start:
  `scripts/sunday-upgrade.sh`. Left untouched per the two-exception rule —
  it is a real, already-diagnosed-and-fixed bug in the launcher itself (the
  `--allowedTools` variadic-argv-consumption bug documented in its own diff,
  plus a dirty-tree/divergence false-FATAL fix), not something this run's
  mission needed to touch. **Flagging for the operator: this fix is good and
  should be committed/reviewed separately** — it is the reason today's
  earlier same-day attempts (visible in `reports/upgrade-run-2026-09-17.log`)
  never got a usable mission into the composer.
- `graphify-out/**` carried no uncommitted state this run (68 local-only
  commits already existed on `main` at session start, all
  `chore(graphify)` batch commits from 16:20–16:23 today, already committed
  — not part of this run's dirty-tree check).
- This run's own work is on branch `chore/upgrade-2026-09-17`, one commit
  (`0c9c015e77`), not pushed, not merged.

## 7. Security findings

### 7a. Image scan (Trivy, CRITICAL+HIGH, prod's currently-running tags)

```
$ bash scripts/sunday-upgrade-apply.sh --env prod --phase scan
agentshroud-gateway:1.6.1   ->  2 CRITICAL / 174 HIGH
agentshroud-openclaw:1.6.1  -> 17 CRITICAL / 225 HIGH
agentshroud/hermes:1.6.1    ->  3 CRITICAL / 398 HIGH
scan: PASS (3 image(s) scanned) — total CRITICAL: 22
```
Full JSON: `reports/sunday/2026-09-17/scan.json`. `--max-critical` was not
set (script default: report-only, does not gate), consistent with the
procedure's "collect findings before upgrading" step — no upgrade is
happening today, so there's nothing to gate.

Hermes carries the largest HIGH count (398) and is also the one component
confirmed behind latest (§5) — the two facts are almost certainly related
and reinforce that Hermes is the priority item for the next dev run.

### 7b. `npm audit` — browser-extension

```
$ (cd browser-extension && npm audit --json)
{"info":0,"low":0,"moderate":0,"high":0,"critical":0,"total":0}
```
Clean.

### 7c. Python dependency audit — GAP, not silently skipped

`pip-audit` in the conda `gsdl` env is broken (`ModuleNotFoundError:
pip_audit`). Ran via `uvx pip-audit` instead (ephemeral, no env mutation) —
but `gateway/` has no `requirements*.txt` or lockfile, and
`uv pip compile gateway/pyproject.toml` resolved to an empty file, so
`--path gateway` had nothing to scan against (`{"dependencies": [],
"fixes": []}` — empty result, not a clean-scan result; do not read this as
"0 vulnerabilities"). **Genuine gap: gateway's Python dependencies were not
scanned this run.** Follow-up: either commit a resolvable lockfile for
`gateway/pyproject.toml` or point `pip-audit` at the actual installed
environment (venv/container) the gateway runs from.

### 7d. Wazuh/SOC alerts — partial

`agentshroud-wazuh-agent` container is `running (healthy)`, up 23h (recent
restart). Checked `/var/ossec/logs/alerts/alerts.log` inside the container
(empty/not found) and the gateway's `/soc/v1/alerts` HTTP path (`404 Not
Found`, gateway itself reports `{"status":"healthy","version":"1.6.1"}` on
`/status`). Did not locate the correct SOC alert path within budget — this
is a tooling-discovery gap, not a confirmed "zero alerts" result. Follow-up:
document the actual SOC alert query path in `CLAUDE.md` or point future runs
at it directly.

### 7e. Base images (Dockerfiles) — inventoried, not re-verified against registry

```
FROM ${HERMES_IMAGE}                                                    # tracked in §5
golang:1.26-trixie@sha256:771f3162...        (cosign/docker-cli/falcoctl/slsa builders)
node:24-bookworm-slim@sha256:2fe369e9...
python:3.12-slim@sha256:c3d81d25...
python:3.13-slim@sha256:bffeb7bd... (x2, incl. builder stage)
```
All are already digest-pinned (good practice, satisfies supply-chain intent
independent of tag drift). Whether newer digests exist for these tags was
**not checked this run** (would need a `docker manifest inspect`/registry
query per image) — flagged as a follow-up for whichever run next has apply
authority, since a digest bump here is a real code/build change subject to
the same dev-first gate as everything else.

## 8. Security findings table

| ID/Component | Severity | Component | Before | After | Status |
|---|---|---|---|---|---|
| Trivy CRITICAL (22 total) | CRITICAL | gateway/openclaw/hermes 1.6.1 images | 22 CRITICAL / 797 HIGH | unchanged | **OPEN** — no upgrade applied this run; see §7a for per-image breakdown |
| 75 OpenClaw GHSA advisories | Mixed | `gateway/security/agent_cve_registry.py` | `status: under_review` ×75 | `status: fully_mitigated` ×75, each with cited `defense_layers` | **FIXED** (committed `0c9c015e77`) — every advisory already has an upstream fix at/below the running pin (2026.9.4) and/or a real AgentShroud control; zero `partially_mitigated`/`not_mitigated` gaps found by the deterministic triage engine |
| gateway Python deps | Unknown | `gateway/` | n/a | n/a | **OPEN / NOT SCANNED** — see §7c, genuine tooling gap |
| browser-extension npm deps | — | `browser-extension/` | 0/0/0/0 | 0/0/0/0 | **CLEAN** |
| Wazuh/SOC alerts | Unknown | `wazuh-agent` sidecar | n/a | n/a | **OPEN / NOT LOCATED** — see §7d |

## 9. Upgrade table

| Component | Env | Before | After | Status | Notes |
|---|---|---|---|---|---|
| OpenClaw | prod | `2026.9.4` | `2026.9.4` | **ALREADY CURRENT** | verified live via `discover_upstream_versions.py` against npm; last real bump was 2026-09-16, already on `origin/main` |
| Hermes | prod | digest `sha256:68e15ae2...` (`0.20.1`) | *(candidate: `v2026.9.14`, digest `sha256:99641e57...`)* | **BLOCKED-awaiting-dev** | dev handoff missing/FAIL for today; prod may not promote a version dev hasn't verified. Prior history: blocked 2026-08-30 on an A2A compatibility probe failure — unclear if resolved, dev's `verify` phase is the only place that gets checked |
| AgentShroud (self) | prod | `1.6.1` | `1.6.1` | **ALREADY CURRENT** | matches `docker/versions.env`, running gateway reports `1.6.1` via `/status` |
| Wazuh sidecar, Falco, ClamAV, Fluent Bit (in-process in gateway per CLAUDE.md §7.6) | prod | — | — | **NOT INDEPENTLY VERSION-CHECKED** | no separate version pin found outside `AGENTSHROUD_VERSION`; these ship inside the gateway image build, so their currency is tied to the gateway image itself, which is current |
| docker-socket-proxy | prod | `tecnativa/docker-socket-proxy@sha256:1f5038b5...` | not checked against registry this run | **SKIPPED** | digest-pinned already; registry re-check not performed within budget |
| Golang/Node/Python base images | prod (build-time only) | see §7e | not checked against registry this run | **SKIPPED** | digest-pinned already; registry re-check not performed within budget |
| LibreChat (mongo:8.0.32, meilisearch:v1.53.1) | prod (running, untracked in repo) | running versions confirmed via `docker ps` | — | **OUT OF SCOPE / GAP** | no compose file, Dockerfile, or `versions.env` entry in this repo references these two running containers (`librechat-mongodb`, `librechat-meilisearch`) — they exist on the host but this repo has no single-source-of-truth pin for them, so there is nothing for this job's version-bump flow to act on. Flagging as a real gap: either bring them under `docker/versions.env` management or document where they're actually managed. |
| searxng | prod (running, untracked in repo) | `searxng/searxng:2026.8.29-d226b78bc` | not checked | **OUT OF SCOPE / GAP** | same as LibreChat — running via `searxng-local` container, no compose/version-pin reference found in this repo |
| Jira tracking (§6 of the mission procedure) | — | — | — | **UNAVAILABLE** | see §10 |

## 10. Jira — unavailable this run

- `mcp__atlassian-idallasj__jira_search` → `Error calling tool 'search': Failed to configure OAuth session` (matches prior known-broken state).
- `mcp__atlassian-agentshroud__jira_search` and `jira_get_all_projects` → no error, but return **zero projects and zero issues**, including for a bare `project = SCRUM` query that should return many existing tickets. This is a *new* symptom (previously documented as "broken OAuth"; today it authenticates but sees nothing) — worth a human checking cloudId/site mapping for this connector.
- Per the mission's rule ("If the Atlassian MCP tools are unavailable... say so explicitly... never silently skip"): **no SCRUM ticket was created or commented on for this run.** No prior run's ticket could be found to attach to either.

## 11. Verification evidence

No `apply`/`verify` phase ran (nothing was applied), so there is no
post-upgrade health check to show — prod's containers were never touched.
For completeness, prod's state at both the start and end of this run:

```
$ docker compose -p agentshroud --profile full ps
gateway              healthy   (image sha256:80c7d1197f98..., unchanged)
openclaw             healthy   (image sha256:f95b8fe4ce8a..., unchanged)
voice-gateway        healthy   (unchanged)
wazuh-agent          healthy   (agentshroud-wazuh-agent:1.6.1, unchanged)
docker-socket-proxy  running   (unchanged)
hermes (agentshroud-hermes-v2, standalone, not part of `ps --profile full`)
                     healthy   (agentshroud/hermes:1.6.1, unchanged)

$ curl -s localhost:8080/status
{"status":"healthy","version":"1.6.1"}
```
Identical before and after — as expected for a run that applied nothing.

## 12. Breaking changes / manual follow-ups

1. **Dev must produce a passing `dev-result-2026-09-17.json` (or a later
   date) before prod can promote anything.** This is the actual blocker —
   everything else in this report is secondary to it.
2. **Hermes is the priority once dev passes**: real newer image exists
   (`v2026.9.14`), largest HIGH-severity count of the three prod images
   (398), and has a documented history of failing its A2A compatibility
   probe on upgrade attempts. Dev's `verify` phase needs to actually clear
   that probe, not just report `PASS` on the mechanical gates.
3. **Prod Colima disk at 90% (16GiB free, needs ≥25GiB for the next
   `apply`)** — recommend `docker builder prune -f` before the next attempt;
   full details and safer/riskier options in §4.
4. **`gateway/` has no scannable Python dependency lockfile** — `pip-audit`
   found nothing to scan, not "nothing vulnerable." Real gap, see §7c.
5. **Wazuh/SOC alert review is not wired into this account's ability to
   query it** — the documented `/soc/v1/...` path returned 404; someone
   needs to confirm the real path/port and I'll wire discovery of it into
   the script/runbook.
6. **`atlassian-agentshroud` Jira connector now authenticates but returns
   zero projects/issues** — different symptom than the previously-documented
   broken-OAuth state; needs a human to check cloudId/site config.
7. **`scripts/sunday-upgrade.sh` has a good uncommitted fix sitting in the
   working tree** (the launcher `--allowedTools`/dirty-tree/divergence
   fixes) — left untouched per the ground rules since it wasn't this run's
   file to touch, but it fixed the exact bug that stopped every earlier
   same-day attempt from submitting. Recommend the operator review and
   commit it.
8. **LibreChat's datastores (`librechat-mongodb`, `librechat-meilisearch`)
   and `searxng-local` run on this host with no version pin or compose file
   anywhere in this repo** — outside this job's ability to act on; flagging
   so ownership gets assigned.
9. Base images (`golang:1.26-trixie`, `node:24-bookworm-slim`,
   `python:3.12-slim`, `python:3.13-slim`) and `docker-socket-proxy` were
   inventoried but not re-checked against their registries for newer
   digests this run — do so before the next `apply` phase runs.

## 13. Rollback instructions

**Nothing was applied to prod or dev; there is nothing to roll back on
either running stack.** The only state this run changed:

- Two files committed on branch `chore/upgrade-2026-09-17` (commit
  `0c9c015e77`): `docs/security/cve-triage-gaps.md`,
  `gateway/security/agent_cve_registry.py`. To revert:
  ```
  git -C ~/Development/agentshroud revert 0c9c015e77
  ```
  (branch is not merged to `main` and was never pushed, so a plain
  `git branch -D chore/upgrade-2026-09-17` after switching back to `main`
  is also sufficient if the branch itself should simply be discarded).
- No image was built, no container was recreated, no `docker/versions.env`
  pin was changed.

## 14. Time spent

Session start 18:25 EDT, this report written ~18:50 EDT — **~25 minutes**,
well under the 90-minute budget. The run ends here rather than continuing to
poll for a dev result: dev's cadence is out of this session's control, and
per the mission's ground rules a missing/FAIL handoff means stopping after
inventory/scan, not waiting.

---
Both stacks left healthy and untouched. No secrets logged. No destructive
action taken.
