# AgentShroud Sunday Upgrade — 2026-09-26 (DEV run)

Run identity: `whoami` → `agentshroud-bot` → **DEV**
Session: Remote Control / launchd-style unattended run.
Run started (local): 2026-09-26 03:00 ET.
Starting commit: `bab40d1736` (main) — carries all of yesterday's (2026-09-25) run #2 fixes; dev-result for 2026-09-25 was `status=PASS`, all components already-latest as of that run.
Branch: `chore/upgrade-2026-09-26`.

## §0 Preflight

Working tree: clean except pre-existing untracked report files from 09-20..09-25 (`reports/.upgrade-*.done`, `reports/upgrade-2026-09-2{0,1,2,3,4,5}*.md`) — left untouched per the sanctioned-exception rule.

`scripts/sunday-upgrade-apply.sh --env dev --phase preflight`: PASS — docker daemon reachable, compose config valid, 339GiB host free / 124GiB docker storage free.

## §1 Environment mapping

- **dev** = this account (`agentshroud-bot`), compose files `docker/docker-compose.yml` + `docker/docker-compose.agentshroud-bot.marvin.yml`, project `agentshroud-bot`, profile `full`.
- **prod** = separate macOS account (`ijefferson.admin`) on the same host (marvin), separate Colima VM, coordinated via `/Users/Shared/agentshroud-sunday/dev-result-YYYY-MM-DD.json`.
- Services (from `docker/docker-compose.yml`): `gateway` (built from `gateway/Dockerfile`, in-process falco/clamav/fluent-bit per the March 2026 consolidation), `wazuh-manager` (vendor image, isolated internal network), `wazuh-agent` (own Dockerfile, split sidecar per 2026-09-06), `docker-socket-proxy` (digest-pinned vendor image), `openclaw` (`docker/bots/openclaw/Dockerfile`), `hermes` (`docker/bots/hermes/Dockerfile`, `FROM ${HERMES_IMAGE}`), `voice-gateway` (`voice_gateway/Dockerfile`).
- No LibreChat, searxng, or a separate Falco/Fluent Bit container exist in this repo (confirmed via repo-wide grep) — not applicable here, not silently skipped.
- `containers/` and `docker/Dockerfile.agentshroud`(.bak) exist but are referenced by no compose file, Makefile, or script (confirmed via grep) — legacy/dead, excluded from the active inventory below; flagged in §6.

## §2 Inventory & versions checked

| Component | Before | After | Source checked | Status |
|---|---|---|---|---|
| OpenClaw | 2026.9.6 | 2026.9.6 | `discover_upstream_versions.py` (npm:openclaw) | ALREADY CURRENT |
| Hermes | digest `fca358f1...` / v2026.9.24 | same | `discover_upstream_versions.py` (dockerhub:nousresearch/hermes-agent) | ALREADY CURRENT |
| python:3.13-slim (gateway builder+runtime, wazuh-agent) | `sha256:8d9d0b8b...` | `sha256:7c61056e...` | Docker Hub registry API token+manifest digest lookup, 2026-09-26 | UPGRADED |
| python:3.12-slim (voice-gateway) | `sha256:2f17fc04...` | `sha256:f77ac9e4...` | same method | UPGRADED |
| golang:1.26-trixie | `sha256:bdca99a0...` | same | same method | ALREADY CURRENT |
| golang:1.27-trixie | `sha256:433790e5...` | same | same method | ALREADY CURRENT |
| node:24-trixie-slim | `sha256:8ec5d755...` | same | same method | ALREADY CURRENT |
| tecnativa/docker-socket-proxy:latest | `sha256:1f5038b5...` | same | same method | ALREADY CURRENT |
| wazuh-manager / wazuh-agent | 4.14.7 | 4.14.8 | packages.wazuh.com apt `Packages` index (confirmed `4.14.8-1` exists before bumping) | UPGRADED |
| SLSA-verifier | v2.7.1 | v2.7.1 | `gh api repos/slsa-framework/slsa-verifier/releases/latest` | ALREADY CURRENT (rebuilt from source w/ overridden transitive deps — see gateway/Dockerfile comments, unchanged this run) |
| falcoctl | 0.14.2 | 0.14.2 | `gh api repos/falcosecurity/falcoctl/releases/latest` | ALREADY CURRENT |
| cosign (builder + bootstrap 2.6.5) | v3.1.3 | v3.1.3 | `gh api repos/sigstore/cosign/releases/latest` | ALREADY CURRENT |
| syft | v1.52.0 | v1.52.0 | `gh api repos/anchore/syft/releases/latest` | ALREADY CURRENT |
| trivy | v0.74.0 | v0.74.0 | `gh api repos/aquasecurity/trivy/releases/latest` | ALREADY CURRENT |
| falco | 0.45.0 | 0.45.0 | `gh api repos/falcosecurity/falco/releases/latest` | ALREADY CURRENT |
| docker-cli (builder, ARG DOCKER_CLI_VERSION) | 29.8.1 | 29.8.1 | `gh api repos/docker/cli/tags` (repo has no GH "latest" Release object) | ALREADY CURRENT |
| Fluent Bit (ARG FLUENT_BIT_VERSION) | 5.1.2 | 5.1.2 | `gh api repos/fluent/fluent-bit/releases/latest` | ALREADY CURRENT |
| ClamAV / OpenSCAP (gateway image, apt packages) | Debian trixie repo current | same | installed via floating `apt-get install` on every rebuild — mechanically always "latest in trixie" as of build time; no separate version pin exists to bump | ALREADY CURRENT (by construction) |
| gateway/requirements.txt (Python) | — | — | `uvx pip-audit -r gateway/requirements.txt` | CLEAN — 0 known vulnerabilities |
| chatbot/requirements.txt (Python) | — | — | `uvx pip-audit -r chatbot/requirements.txt` | CLEAN — 0 known vulnerabilities |
| voice_gateway/requirements.txt (Python, installed set) | pip 25.0.1 / setuptools 78.1.0 | pip/setuptools upgraded explicitly in Dockerfile | OSV `querybatch` API against `pip list --format=freeze` from the built image (pip-audit's own resolve hung repeatedly in this sandbox) | FIXED — see below |
| browser-extension (jest) | 29.7.0 | 30.5.2 | `npm audit` (clean at both versions) + `npm view jest version` | UPGRADED, tests re-verified (32/32 pass) |
| lvgl_kawaii_face (git submodule, `idallasjlabs/lvgl_kawaii_face`) | pinned commit, 4 commits past tag `v1.0.0` | unchanged | `git fetch --tags` in the submodule; no tag newer than `v1.0.0` exists, and the pinned commit is already ahead of it | ALREADY CURRENT (own vendored repo, no newer release to move to) |
| Colima / Docker CLI / docker-compose (host tooling) | colima 0.10.3, docker 29.8.1, docker-compose 5.5.1 (via `brew list --versions`) | unchanged | `brew outdated` could not run — `/opt/homebrew/Cellar` not writable by this account | REPORTED ONLY, not owned by this job per ground rules |

### Wazuh advisory registry (via `scripts/discover_upstream_versions.py` / GHSA sync)

- `scripts/sync-cve-registry.py --source ghsa`: **0 new advisories** across all 10 registries (openclaw, hermes, trivy, cosign, openscap, syft, falco, clamav, wazuh, semgrep) — all already up to date with their upstream GHSA feeds.
- `scripts/triage-cve-mitigations.py --agent openclaw`: 0 under_review, 0 newly-triaged. Registry stands at 889 fully_mitigated / 1 partially_mitigated (`ASH-OCLAW-113`) / 1 under_review (`ASH-OCLAW-230`) — both pre-existing residuals from prior runs, unchanged today.
- `scripts/triage-cve-mitigations.py --all-agents --dry-run`: confirms a **pre-existing architecture gap**, not something broken today — the engine only has a running-version source wired for `openclaw`. 98 advisories across the other 9 registries (hermes 0, trivy 5, cosign 10, openscap 0, syft 2, falco 8, clamav 0, wazuh 73, semgrep 0) cannot be triaged at all and stay structurally UNTRIAGED. The script itself surfaces this loudly (exit 1, explicit per-agent SKIPPED lines) rather than hiding it — see §6.

## §3 Bugs found and fixed this run (code changes, per owner directive — a needed fix is not grounds to skip)

1. **`scripts/lib/sunday-scan.sh` zero-CRITICAL crash** — already fixed in yesterday's run (`bab40d1736`), inherited clean.
2. **`scripts/sync-cve-registry.py` NVD keyword not URL-encoded** — `fetch_nvd_cves()` built `?keywordSearch=nousresearch hermes` with a literal space; Python 3.14's urllib now rejects it (`InvalidURL: control characters`), hard-failing `--source nvd`/`--source all`. Fixed with `urllib.parse.quote()`. This had evidently been broken for some time — see finding below.
3. **Same run surfaced a second, more serious bug, deliberately NOT fixed today**: once the encoding was fixed, the NVD source's registry-append logic wrote Hermes's 39 "new" CVE entries into `_SEMGREP_CVE_REGISTRY` instead of `_HERMES_CVE_REGISTRY` (wrong list — an insertion-marker bug), and separately failed to append OpenClaw's 669 "new" entries at all ("registry marker not found — cannot append"). **Caught via `git diff` before any commit — reverted with `git checkout -- gateway/security/agent_cve_registry.py`, nothing corrupted landed.** This needs its own investigation and is tracked as a manual follow-up (§6), not rushed under this run's clock. GHSA sync (the script's primary/more-trusted source per its own docstring) is unaffected and ran clean.
4. **voice-gateway pip/setuptools** — see inventory table; fixed with an explicit `pip install --upgrade pip setuptools` layer, since the base-digest refresh alone does not carry a newer pip (verified by pulling the new digest directly and checking).

## §4 Build / apply / verify (dev)

**Run #1** — `scripts/sunday-upgrade-apply.sh --env dev --phase all`, launched 03:25:51 ET against commit `cc9de71925` (python/wazuh/jest/pip-setuptools bumps; the NVD-sync fix and the CVE-2026-43185 accept-risk entry landed as later commits, both non-image-affecting — see below).

- `baseline`: PASS — rollback anchor written, 5 images retagged `agentshroud-rollback/<container>:2026-09-26`, `rollback.sh` generated.
- `scan`: PASS — 3 images scanned (gateway/openclaw/hermes), 0 scans failed.
- `apply`: PASS at 04:10:55 ET (build + `docker compose up` + hermes standalone start completed; full real rebuild of gateway/openclaw/hermes/wazuh-agent/voice-gateway — expected, since the python:3.13-slim/3.12-slim digest bumps and wazuh 4.14.8 bump invalidate those layers). `post-deploy-check`: **11/11 checks passed** (gateway `/status` 200, bot container up clean, no V8 stack overflow, no Slack `invalid_auth`, no `Failed to start CLI`, Hermes dashboard :9119 200, Hermes API :8642 reachable, no Hermes startup crash, no Docker network pool overlap, sandbox image present, local model registered).
- `verify`: PASS at 04:12:56 ET — all 5 compose-managed containers + hermes-standalone healthy and stable through a 120s restart-loop soak. `no-op gate: NO-OP (honest) — already on the latest upstream release; nothing to upgrade` (OpenClaw/Hermes specifically — correct, since neither moved this run; other components genuinely did move, see §2).
- `handoff`: wrote `/Users/Shared/agentshroud-sunday/dev-result-2026-09-26.json`, `status=PASS`.
- Total run #1 wall time: ~45 min (03:25:51 → 04:13:00).

**Post-run #1 fixes** (commits `90767b6747`, `c424bb10c1`) surfaced a residual CRITICAL in the hermes image scan (`agentshroud/hermes:1.7.0` scan showed 1 CRITICAL — `CVE-2026-43185`, linux-libc-dev, verified unreachable and no fix exists, see §2/§3). Ran `--phase scan` standalone (04:15:10–04:16:31 ET) against the new `.trivyignore.yaml` entry: **confirmed 0 CRITICAL across all 3 images** before committing the entry.

**Run #2** — re-ran `--phase all` (04:17→04:22 ET) to get a dev-result.json published from the exact final HEAD. Docker's build cache had been evicted for the Go-module-heavy builder stages (falcoctl/cosign/slsa-verifier — each does a fresh `go mod tidy`/download regardless of instruction-level cache, and the run #1 cleanup step bounded total build cache to 20GiB) — the run began re-downloading Go modules from scratch, which would have meaningfully exceeded the 90-minute budget for zero behavioral gain, since neither of the two commits since run #1 touch any image input (script logic and a trivy accept-risk config entry only). **Killed at 04:21:58 ET, before it reached the `apply` stage** — confirmed via log tail it was still inside the gateway Dockerfile build (step 56/125, `docker/daemon.json` layer) and had not touched any running container. Verified directly instead:
  - `docker-compose ... ps` at 04:22:30 ET: all 6 services (`gateway`, `openclaw`, `wazuh-agent`, `wazuh-manager` on `4.14.8`, `voice-gateway`, `docker-socket-proxy`) **Up and healthy**, running the run #1 images, untouched by the killed run.
  - `docker ps --filter name=agentshroud-dev-hermes`: **Up 17 minutes (healthy)**.
  - `reports/sunday/2026-09-26/baseline.json` (git_rev `c424bb10c1`, from the killed run's completed baseline step, before it was killed) — this IS a real machine-written baseline at the final commit, giving accurate rollback anchors even though the run didn't finish.

**Reconciliation, per CLAUDE.md §0.0 rule 5** (trust the machine's record, but re-read it honestly): the published `dev-result-2026-09-26.json` records `status=PASS` at run #1's commit (`cc9de719254b`), one commit behind current HEAD (`c424bb10c1`). The delta between those two commits is exactly: (a) a Python script fix (`sync-cve-registry.py`, invoked manually, not part of any Docker build or running container) and (b) one `.trivyignore.yaml` accepted-risk entry (consumed only by the scan gate, not baked into any image). Neither can affect container health. The scan gate at HEAD was independently re-verified clean (0/0/0 CRITICAL, evidence above) rather than assumed. I did not hand-edit `dev-result-2026-09-26.json` (the Sunday-upgrade instructions are explicit that only the script may write it) — treat the published JSON's `git_rev` as one commit stale on a script/config-only delta that was separately verified, not as a discrepancy to paper over.

## §5 Security findings

| ID | Severity | Component | Before | After |
|---|---|---|---|---|
| CVE-2026-4xh5 family (pip) — GHSA-4xh5-x5gv-qwph + 5 more | HIGH/CRIT mix | voice-gateway (python:3.12-slim bundled pip 25.0.1) | OPEN | **FIXED** — explicit `pip install --upgrade pip setuptools` in `voice_gateway/Dockerfile`; fixed in pip 25.3 |
| GHSA-5rjg-fvgr-3xxf (setuptools path traversal) | HIGH | voice-gateway (setuptools 78.1.0) | OPEN | **FIXED** — same commit; fixed in setuptools 78.1.1 |
| CVE-2026-43185 (ksmbd signedness bug, via `linux-libc-dev`) | CRITICAL | `agentshroud/hermes:1.7.0` (newly surfaced by today's python:3.13-slim digest refresh) | OPEN | **ACCEPTED** — `.trivyignore.yaml`, expires 2026-11-10; no fix in Debian trixie, package is headers-only (no ksmbd code), no image runs/exposes SMB |
| CVE-2026-6653 (libxml2 UAF) | CRITICAL | `agentshroud/hermes:1.7.0` | ACCEPTED (2026-09-25) | unchanged — ACCEPTED, expires 2026-10-27 |
| CVE-2026-60002 (openssh-client) | CRITICAL | `agentshroud/hermes:1.7.0` | ACCEPTED (prior run) | unchanged — ACCEPTED, expires 2026-12-24 |
| jest supply-chain surface (no active CVE, but a full major behind) | — | browser-extension (devDependency only) | OPEN (29.7.0) | **FIXED** — bumped to 30.5.2, `npm audit` clean before and after, 32/32 tests pass |
| Wazuh manager/agent | — | 4.14.7 | **FIXED** — 4.14.8 |
| `sync-cve-registry.py` NVD source hard-failing (`InvalidURL`) | — | CVE-sync tooling | OPEN, silent (broken for an unknown prior period) | **FIXED** — `urllib.parse.quote()` on the keyword |
| `sync-cve-registry.py` registry-append writes to the wrong agent's list / fails outright | — | CVE-sync tooling (`gateway/security/agent_cve_registry.py` append path) | **NEWLY DISCOVERED**, not present before (only reachable once the encoding bug above was fixed) | **NOT FIXED THIS RUN** — caught via `git diff` before commit, reverted, zero corrupted data landed. Tracked as a manual follow-up (§6); GHSA sync (primary source) unaffected |
| `scripts/triage-cve-mitigations.py` has no running-version source for 9/10 agents (hermes, trivy, cosign, openscap, syft, falco, clamav, wazuh, semgrep) | — | CVE-triage tooling | **PRE-EXISTING**, confirmed still true today (98 advisories structurally UNTRIAGED, script self-reports this loudly, exit 1) | **NOT FIXED THIS RUN** — architecture gap, needs real per-agent wiring, not a version bump; tracked as follow-up (§6) |
| OpenClaw CVE registry (891 total) | — | openclaw | 889 fully_mitigated / 1 partial / 1 under_review | unchanged — re-triaged, 0 new, both residuals pre-existing from prior runs |
| GHSA sync, all 10 agent registries | — | all | — | **0 new advisories** — confirmed up to date against every agent's own GHSA feed |

## §6 Breaking changes / manual follow-ups

1. **Jira ticket for this run could not be created.** `mcp__atlassian__createJiraIssue` was denied by this session's permission mode ("Claude Code is running in don't ask mode"). Per CLAUDE.md §6, recording this explicitly rather than silently skipping: no SCRUM ticket exists for the 2026-09-26 run. A human should either re-run this step with write access, or manually log this run under SCRUM.
2. **`sync-cve-registry.py`'s NVD-source registry-append logic is broken** and needs real investigation before `--source nvd`/`--source all` is trusted again: (a) OpenClaw's append failed outright ("registry marker not found — cannot append"); (b) Hermes's append wrote 39 entries into `_SEMGREP_CVE_REGISTRY` instead of `_HERMES_CVE_REGISTRY`. Root cause not yet identified (likely a fragile text-marker match in `rewrite_registry_text`-style logic, going by the sibling `triage-cve-mitigations.py`'s docstring describing the same append pattern). GHSA sync (the script's primary source per its own docstring) is unaffected. Do not run `--source nvd`/`--source all` unattended again until this is fixed.
3. **`triage-cve-mitigations.py` covers only `openclaw`.** 98 advisories across the other 9 agent/tool registries cannot be triaged (`--all-agents --dry-run` confirms, exit 1, itemized per agent in §2). This is real backlog work (per-agent running-version source wiring), not a version-pin issue — flagged as a GAP per owner directive, not something to record and move past.
4. **`containers/agentshroud/Dockerfile`, `containers/gateway/Dockerfile`, `docker/Dockerfile.agentshroud(.bak)` are dead code** — referenced by no compose file, Makefile, or script (confirmed via repo-wide grep). Not part of this run's active inventory. Flagging so nobody assumes they're covered by weekly upgrades; either wire them in or delete them.
5. **No BLOCKED items this run** — nothing required loosening a security control. Every planned bump shipped.
6. **Host tooling** (`colima` 0.10.3, `docker` 29.8.1, `docker-compose` 5.5.1) reported only, not upgraded — `brew outdated` could not run (`/opt/homebrew/Cellar` not writable by this account) and CLAUDE.md/docs do not say the Sunday job owns host tooling.
7. **Observed, not acted on**: the prod account's (`ijefferson.admin`) own long-running session on this host carries continuation notes (dated 2026-09-25) indicating a prod rebuild was left PENDING a manual go/no-go decision around a planned host reboot, and that OpenClaw's one-way config migration (2026.9.6) makes any rollback to a pre-2026.9.6 image on prod dangerous unless `sunday-upgrade-apply.sh` is at or past commit `e7874cc35c` (the rollback-refuses-a-brick guard). This is prod's own account/state, outside dev's reach or remit — surfaced here only so whoever runs today's prod job has it, since dev cannot post it anywhere prod-side directly.

## §7 Rollback instructions

Machine-generated, current as of HEAD `c424bb10c1` (`v1.7.0-70-gc424bb10c1`):

```bash
bash /Users/agentshroud-bot/Development/agentshroud/reports/sunday/2026-09-26/rollback.sh
```

This restores `docker/versions.env` to the pre-run baseline, retags the 5 baselined images back from `agentshroud-rollback/<container>:2026-09-26`, and recreates containers via `docker-compose ... up -d --force-recreate` (hermes handled separately via `scripts/asb up hermes`, since it's not compose-managed). Manual equivalent if the script is unavailable:

```bash
cd /Users/agentshroud-bot/Development/agentshroud
git checkout bab40d1736 -- docker/versions.env docker/docker-compose.yml gateway/Dockerfile docker/wazuh-agent/Dockerfile voice_gateway/Dockerfile browser-extension/package.json browser-extension/package-lock.json .trivyignore.yaml scripts/sync-cve-registry.py
docker tag agentshroud-rollback/agentshroud-dev-gateway:2026-09-26 agentshroud-gateway:1.7.0
docker tag agentshroud-rollback/agentshroud-dev-openclaw:2026-09-26 agentshroud-openclaw:1.7.0
docker tag agentshroud-rollback/agentshroud-dev-hermes-v2:2026-09-26 agentshroud/hermes:1.7.0
docker tag agentshroud-rollback/agentshroud-dev-voice-gateway:2026-09-26 agentshroud-bot-voice-gateway:latest
docker-compose -f docker/docker-compose.yml -f docker/docker-compose.agentshroud-bot.marvin.yml -p agentshroud-bot --profile full up -d --force-recreate gateway openclaw wazuh-agent wazuh-manager voice-gateway
./scripts/asb up hermes
```

Not exercised this run — dev never failed, so no rollback was needed. Listed for completeness per the ground rules.

## §8 Time spent

Started 03:00:16 ET, this write-up completed ~04:28 ET: **~88 minutes**, within the 90-minute budget (close to it — the killed, redundant second `--phase all` run is the reason; see §4). Both stacks (dev — the only one this account can reach) left healthy. No prod action taken or attempted by this account (cross-account boundary, see cross-account reality in the mission brief).

## §9 08:00 ET relaunch reconciliation (separate session)

The 03:00 session above (§0–§8) finished its real work at ~04:28 ET but never
caused its own tmux/`claude --remote-control` process to exit. `scripts/sunday-upgrade.sh`'s
completion sentinel (`reports/.upgrade-2026-09-26.done`) is written by the
**wrapper**, only when the tmux session ends naturally before its 180-minute
watch timeout (`scripts/sunday-upgrade.sh:414-423`) — never by the Claude
session itself, and there is no sanctioned tool available to a Claude Code
session to end its own conversation once a task is finished (`EndConversation`
is explicitly restricted to sustained user abuse / demonstration, and its own
tool description explicitly says NOT to use it when "it has finished a task").
So the 03:00 session sat idle post-report until the wrapper's watch loop hit
180 minutes at 06:00:41 ET, force-killed the tmux session, logged
`TIMED OUT — no sentinel written`, then its own leftover-checkout handler found
the working tree on `chore/upgrade-2026-09-26` with the report file still
untracked (uncommitted) — so it force-pushed the branch as-is ("work preserved
for review") and reset the checkout to `main`. No sentinel exists, so the next
scheduled fire (08:00:04 ET) relaunched a **new** session — this one — per the
"report exists but no completion sentinel → treat as incomplete, re-run" rule
at `scripts/sunday-upgrade.sh:270-271`.

**This session verified, rather than assumed, that no work needed to be
redone** (CLAUDE.md §0.0 — assert on resulting state, not on step completion):

- `docker ps`: all 7 dev containers `Up 4 hours (healthy)` — `wazuh-manager`
  and `wazuh-agent` on `4.14.8`, `hermes-v2` and `openclaw` on image tag
  `1.7.0` — matching §2–§4 above exactly. The 03:00 run's upgrade was real and
  is still running healthy, not a claim that needed re-verifying by rebuilding.
- `git fetch origin` + `git log main..origin/chore/upgrade-2026-09-26`: the six
  commits the 03:00 report describes (`36cf07f41f` digest bumps →
  `3bfa5e8f5f` Wazuh → `be050375c0` jest → `cc9de71925` pip/setuptools →
  `90767b6747` CVE-sync fix → `c424bb10c1` accept-risk entry) exist on the
  pushed branch, local and origin both at `c424bb10c1` — nothing lost.
- `/Users/Shared/agentshroud-sunday/dev-result-2026-09-26.json`: `status=PASS`,
  versions match — the prod handoff artifact the 03:00 run promised was
  genuinely written, not merely claimed.

**Action taken this session**: committed the untracked `reports/upgrade-2026-09-26.md`
onto `chore/upgrade-2026-09-26` (this commit), re-pushed, and returned the
checkout to `main` — the exact two steps the wrapper's own leftover-checkout
handler does, done here explicitly so the branch is left clean instead of
relying on the wrapper to notice. **No rebuild, no re-run of
`sunday-upgrade-apply.sh`, and no changes to running containers** — every
version bump already stands, verified live above.

**Not fixed, flagged instead (§6-style, real follow-up, not papered over)**:
this run has no way to make its own completion legible to the wrapper. Absent
a wrapper-side change (e.g. polling a Claude-written logical-completion marker
file in addition to tmux liveness, then having the *wrapper* kill the session)
or a product-level carve-out letting an unattended automation session signal
"task complete, please end me," **every future run of this job will time out
at 180 minutes and get relaunched, even when it finished correctly in under
90.** This has now recurred on at least three dates (2026-09-20, 2026-09-25,
2026-09-26 — this run) per the wrapper's own committed comments and tonight's
log. This session did not attempt to patch `scripts/sunday-upgrade.sh`'s watch
loop mid-run: it is actively supervising this very process, and changing its
detection logic while it is running is a bigger, riskier change than this
run's actual scope (today's version bumps) warrants without a human decision
on the right fix. Filed as product feedback separately.

Jira logging remains blocked in this session too — launched with
`--allowedTools Bash,Read,Edit,Write,Glob,Grep` (`SUNDAY_UPGRADE_AUTON=1`,
`scripts/sunday-upgrade.sh:306-308`), which does not include
`mcp__atlassian__*`. Confirmed structurally this time, not just observed as a
denial — no SCRUM ticket exists for this run; a human should log it manually
or grant MCP tool access to the automated session.

## Summary line

**ALL GREEN** — every planned component upgrade shipped (Wazuh 4.14.7→4.14.8, python:3.13-slim/3.12-slim base digests, jest 29.7.0→30.5.2, voice-gateway pip/setuptools), all newly-surfaced CVEs FIXED or ACCEPTED with evidence and expiry, dev stack fully healthy and soak-verified, `dev-result-2026-09-26.json` published PASS. Two pre-existing tooling gaps (NVD-sync append bug, 9/10-agent triage coverage gap) were found, NOT papered over, and are tracked in §6 as real follow-up work rather than being fixed under time pressure. Jira logging for this run is blocked by session permissions (§6 item 1) — flagged, not silently skipped. **§9 (added 08:00 ET, separate relaunched session): dev's upgrade work was verified still healthy and unchanged; the only action taken was committing/pushing the previously-uncommitted report and restoring the `main` checkout — no rebuild occurred. The wrapper's 180-minute-timeout-vs-no-self-termination gap that caused this relaunch is unresolved and reported as product feedback; expect this job to keep relaunching every ~2-3h until that's fixed by a human.**
