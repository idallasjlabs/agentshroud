# AgentShroud Sunday Upgrade Report — 2026-09-20 (PROD run)

Session: Remote Control, `ijefferson.admin` (prod) account on marvin.
Started 2026-09-20T11:04:08Z (07:04 EDT). Report written 2026-09-20T12:01Z
(07:01 EDT) — **~57 minutes elapsed**, inside the 90-minute budget.
Repo HEAD at start: `ac843cf090f6c5cbafdacad925a8d3fe14f1881e`.

## 1. Summary line

**PARTIAL (prod promotion BLOCKED-awaiting-dev; read-only preflight/inventory/scan/CVE-triage-review completed; two real findings surfaced this run)**

Nothing was applied to prod's running stack — no version pin changed, no
image built, no container recreated. This is the **fourth consecutive
tracked Sunday-class run** (2026-08-30 manual PASS with Hermes BLOCKED →
2026-09-15 dev FAIL → 2026-09-17 on-demand prod BLOCKED → **today**) where
prod could not promote because dev has not published a passing handoff.
This run's added value over 2026-09-17: (a) direct evidence that **today's
dev run is not merely late but appears hung** (§3b), and (b) a previously
unflagged **98-advisory CVE-triage scope gap** across five security sidecars
(§7c).

## 2. Environment mapping (discovered, not assumed)

| Concept | Evidence |
|---|---|
| This session's account | `ijefferson.admin` (prod), host marvin, confirmed via `whoami` |
| Dev account | `agentshroud-bot`, separate Colima VM, same physical host — this session cannot start/stop/exec into it |
| Dev→prod handoff | `/Users/Shared/agentshroud-sunday/dev-result-<date>.json`, written only by `scripts/sunday-upgrade-apply.sh --env dev` |
| Prod compose stack | `docker/docker-compose.yml`, project `agentshroud`, profile `full` — `gateway`, `openclaw`, `wazuh-manager`, `wazuh-agent`, `docker-socket-proxy`, `voice-gateway`; Hermes (`agentshroud-hermes-v2`) runs standalone via `docker/bots/hermes/run-standalone.sh`, not part of `--profile full` |
| Deterministic script | `scripts/sunday-upgrade-apply.sh --env dev\|prod --phase ...` — the only thing authorized to mutate `docker/versions.env` or rebuild/redeploy |
| Version source of truth | `docker/versions.env` (`AGENTSHROUD_VERSION`, `OPENCLAW_VERSION`, `HERMES_IMAGE`/`HERMES_VERSION`) |

## 3. Dev handoff check (gating decision)

```
$ ls -la /Users/Shared/agentshroud-sunday/
dev-result-2026-08-30.json
dev-result-2026-09-15.json
(no 2026-09-20 file)

$ cat dev-result-2026-09-15.json
{"date":"2026-09-15","status":"FAIL","upgraded":false,
 "latest_openclaw_seen":"2026.9.4",
 "versions":{"AGENTSHROUD_VERSION":"1.6.1","OPENCLAW_VERSION":"2026.7.1-2",
             "HERMES_VERSION":"0.20.1"}}
```

No `dev-result-2026-09-20.json` exists. Most recent handoff (2026-09-15) is
`status: FAIL`. Per the mission's ground rule, **every prod version bump this
run is BLOCKED-awaiting-dev.**

### 3b. New finding — dev's run appears hung, not merely absent

Cross-account `ps` shows (both accounts share this host):

```
agentshroud-bot  13333  9:27.88 CPU  started 2026-09-19 14:28 EDT
  claude --resume agentshroud-dev
agentshroud-bot   7363  started 2026-09-19 14:25 EDT
  tmux new-session -A -d -s asb
agentshroud-bot  49866  started 2026-09-20 07:36 EDT (during this run)
  timeout 900 bash docker/scripts/docker-cleanup.sh safe-prune
```

A `claude --resume agentshroud-dev` process has been running **continuously
since 2026-09-19 14:28 EDT — ~17 hours** as of this report, well past dev's
03:00 ET scheduled start and any reasonable completion time, and has
accumulated 9m27s of CPU time (consistent with an interactive/idle session,
not a crash-looping one). This is the most direct evidence yet for *why* no
`dev-result-2026-09-20.json` was written: the dev session that should have
produced it is still open, not absent and not cleanly failed. I cannot
attach to or inspect this session (different macOS account, out of scope
for a prod-account run) and took no action against it — killing another
account's session is exactly the kind of cross-account action the handoff
contract reserves to a human. **Recommend the operator check the
`agentshroud-bot` tmux session (`asb`) directly and either let it finish or
terminate/restart it before next Sunday.**

## 4. Preflight (prod, read-only)

```
$ bash scripts/sunday-upgrade-apply.sh --env prod --phase preflight
preflight: docker daemon reachable
preflight: compose config valid
preflight: host volume 259GiB free
preflight: docker storage OK (58GiB free)
WARN: 1 uncommitted change (excluding graphify-out) — this run's own new report file
preflight: PASS
```
Disk is healthy this week (259GiB host / 58GiB Docker, vs. 16GiB Docker-storage
that blocked the 2026-09-17 attempt at the `apply` gate) — not a blocker even
if dev had passed.

## 5. Inventory — wrapped agents (mandatory script, cited)

```
$ python3 scripts/discover_upstream_versions.py --json
{
  "openclaw": {"latest": "2026.9.5", "source": "npm:openclaw", "error": null},
  "hermes":   {"latest": "v2026.9.14",
               "digest": "sha256:99641e57ec762c59e54cb44aa6746b7fc68c18b3c5ddb088af54234c613d9294",
               "source": "dockerhub:nousresearch/hermes-agent", "error": null}
}
```

| Component | Pinned (`docker/versions.env`) | Latest stable | Status | Source |
|---|---|---|---|---|
| OpenClaw | `2026.9.4` | `2026.9.5` | **BEHIND — BLOCKED-awaiting-dev** | npm registry, verified live above. One patch release behind (bumped to `.4` on 2026-09-16; `.5` shipped since). |
| Hermes | digest `sha256:68e15ae2...` (labeled `0.20.1`) | tag `v2026.9.14`, digest `sha256:99641e57...` | **BEHIND — BLOCKED-awaiting-dev** | Docker Hub tags, verified live above. Same gap reported 2026-09-17; has a documented history of failing its A2A compatibility probe on prior dev attempts (2026-08-30 dev-result: `"hermes": "BLOCKED ... fails A2A probe"`) — unresolved status unknown, that check only runs in dev's `apply`/`verify` phases. |
| AgentShroud (self) | `1.7.0` | n/a (own product) | **CURRENT** | `docker/versions.env`; matches running `agentshroud-gateway:1.7.0` / `agentshroud-openclaw:1.7.0` / `agentshroud/hermes:1.7.0` images confirmed via `docker ps` |

## 6. Prod stack current health (read-only, unchanged by this run)

```
$ docker ps
agentshroud-gateway        agentshroud-gateway:1.7.0        Up 17h (healthy)
agentshroud-openclaw       agentshroud-openclaw:1.7.0       Up 17h (healthy)
agentshroud-hermes-v2      agentshroud/hermes:1.7.0         Up 17h (healthy)
agentshroud-voice-gateway  agentshroud-voice-gateway        Up 17h (healthy)
agentshroud-wazuh-agent    agentshroud-wazuh-agent:1.7.0    Up 11h (healthy)
agentshroud-wazuh-manager  wazuh/wazuh-manager:4.14.7       Up 17h (healthy)
agentshroud-docker-socket-proxy  (pinned digest)             Up 17h
```
All AgentShroud-owned containers healthy; nothing touched, nothing to roll back.

## 7. Security findings

### 7a. Image scan (Trivy, CRITICAL+HIGH, prod's currently-running tags)

```
$ bash scripts/sunday-upgrade-apply.sh --env prod --phase scan
agentshroud-gateway:1.7.0   -> 2 CRITICAL / 174 HIGH
agentshroud-openclaw:1.7.0  -> 17 CRITICAL / 229 HIGH
agentshroud/hermes:1.7.0    -> 4 CRITICAL / 386 HIGH
scan: PASS (3 image(s) scanned), total CRITICAL: 23
```
Full JSON: `reports/sunday/2026-09-20/scan.json`. No `--max-critical` gate set
(report-only, consistent with "collect findings" — no upgrade is happening).

**Delta vs. the last documented scan (2026-09-17, same tags on 1.6.x images
before the 1.7.0 bump):**

| Image | 09-17 | 09-20 | Change |
|---|---:|---:|---|
| gateway | 2C/174H | 2C/174H | unchanged |
| openclaw | 17C/225H | 17C/229H | HIGH +4 (new upstream Debian/tool advisories; matrix documents this class as `fix: NONE`, not drift) |
| hermes | 3C/398H | 4C/386H | CRITICAL +1, HIGH −12 — **worth a human look**: this is the same pinned digest as 09-17 (`sha256:68e15ae2...`, unchanged), so the CRITICAL increase is the Trivy vulnerability DB picking up a newly disclosed CVE against unchanged image content, not a regression in our config. Per §2b of the matrix's own methodology this should be added to `docs/security/cve-mitigation-matrix.md`'s residual table — not done this run (documentation-only edit, deferred to keep this run read-only pending dev). |

### 7b. `npm audit` — browser-extension

```
$ (cd browser-extension && npm audit --json)
{"info":0,"low":0,"moderate":0,"high":0,"critical":0,"total":0}
```
Clean.

### 7c. AgentShroud CVE-mitigation triage — NEW FINDING: 98-advisory scope gap

Ran `python3 scripts/triage-cve-mitigations.py` (read-only inspection, then
reverted the one line it rewrote — see below) and independently counted the
full `gateway/security/agent_cve_registry.py` registry by ID prefix and
status:

```
ASH-OCLAW    {'fully_mitigated': 891}
ASH-HERMES   {'fully_mitigated': 13, 'partially_mitigated': 1}
ASH-COSIGN   {'under_review': 10}
ASH-FALCO    {'under_review': 8}
ASH-SYFT     {'under_review': 2}
ASH-TRIVY    {'under_review': 5}
ASH-WAZUH    {'under_review': 73}
```

**`scripts/triage-cve-mitigations.py` only triages `bot == 'openclaw'`
entries** (confirmed: this run's invocation printed "Triaged 0 under_review
advisories for 'openclaw'" — correct, because OpenClaw's 891 are already all
`fully_mitigated` from the 2026-09-17 run — while the registry independently
contains **98 `under_review` entries across Cosign, Falco, Syft, Trivy, and
Wazuh that the automated triage pipeline never processes**, some dating to
the 2026-08-23 GHSA sync (~4 weeks stale as of today). One of the Trivy
entries (`ASH-TRIVY-002`, `GHSA-69fq-xp46-6x23` / CVE-2026-33634, **CRITICAL**
— "Trivy ecosystem supply chain temporarily compromised", `fixed_in: 0.69.3`)
is already moot in practice (installed `trivy --version` = **0.74.0**, well
past the fix) but the registry still carries it as `under_review` because
nothing ever closes that record.

This is exactly the §2b "Arm 2" gap the mission calls out — advisories against
AgentShroud's own supporting security tools (several explicitly named as
must-stay-wired in CLAUDE.md §7.6: Falco, Wazuh) have never gone through the
mitigation-coverage triage that OpenClaw's advisories get. **Not fixed this
run** — extending `triage-cve-mitigations.py`'s bot filter (or running it
per-bot) is a real code change to a security-classification tool and belongs
on dev first, same as any other upgrade-flow change, not improvised on a
blocked prod run. Flagged as tracked follow-up, not silently left off the
gap list.

*Housekeeping:* the triage script unconditionally rewrites
`docs/security/cve-triage-gaps.md`'s summary table with **only the current
invocation's** counts (it overwrote "fully_mitigated: 75 / Total triaged: 75"
from 2026-09-17 with "0 / 0" because zero *new* under_review OpenClaw entries
existed to triage today) — this understates the registry's true cumulative
state and I reverted it (`git checkout -- docs/security/cve-triage-gaps.md`)
rather than commit a misleading regression. Worth fixing the script to report
cumulative totals, not last-run totals — noted as a follow-up, not fixed here.

### 7d. Python dependency audit

`pip-audit`/`pip_audit` not installed in this account's environment and not
available via `uvx` within the time budget used on higher-value items this
run (§3b, §7c). Same genuine, previously-documented gap as 2026-09-17 —
`gateway/` ships `requirements.txt` (not a lockfile), so a real pinned-version
audit needs a resolvable environment, not a bare `pip-audit -r
requirements.txt` invocation. **Not scanned this run — a gap, not a clean
result.**

### 7e. Wazuh/SOC alerts

Not independently re-checked this run beyond the registry-level Wazuh-CVE
finding in §7c (73 `under_review` Wazuh advisories — a different thing from
live Wazuh alert output). 2026-09-17 documented the `/soc/v1/alerts` path
404ing; not re-verified today within budget.

### 7f. Base images / other pinned components — inventoried, not re-verified against registry this run

`docker-socket-proxy` (digest-pinned), Golang/Node/Python base images
(digest-pinned in every Dockerfile), Wazuh manager (`wazuh/wazuh-manager:4.14.7`,
pinned, not floating) — all already pinned to explicit tags/digests (good
practice); whether newer digests/tags exist was not re-queried against each
registry this run (same as 2026-09-17 — a registry-per-image check belongs
with `apply` authority, not a blocked read-only run).

### 7g. Untracked host services — confirmed still out of repo scope

`librechat-mongodb` (`mongo:8.0.32`), `librechat-meilisearch`
(`getmeili/meilisearch:v1.53.1`), `searxng-local`
(`searxng/searxng:2026.8.29-d226b78bc`) remain running on this host with **no
compose file, Dockerfile, or `docker/versions.env` entry anywhere in this
repo** — unchanged gap from 2026-09-17, re-confirmed via `docker ps` today.

**New this run:** two containers use **floating, unpinned tags**, which
directly violates the mission's "never leave a component on an unpinned tag"
rule: `ghcr.io/github/github-mcp-server:latest` (4 running instances,
`openclaw-mcp-*`) and `ghcr.io/home-assistant/home-assistant:stable`. Neither
is referenced in any compose file this repo owns, so — like LibreChat/searxng
— there is no `versions.env` hook to pin them from. Flagged as a genuine
open finding, ownership unassigned.

## 8. Security findings table

| ID/Component | Severity | Before | After | Status |
|---|---|---|---|---|
| Trivy image scan (gateway/openclaw/hermes, 1.7.0 tags) | 23 CRITICAL / 789 HIGH total | unchanged (no rebuild this run) | unchanged | **OPEN** — see §7a for per-image deltas vs. 09-17 |
| ASH-COSIGN ×10, ASH-FALCO ×8, ASH-SYFT ×2, ASH-TRIVY ×5, ASH-WAZUH ×73 (98 total) | Mixed, incl. 1 CRITICAL | `under_review` | `under_review` (unchanged) | **OPEN / NEW FINDING** — triage pipeline scope gap, see §7c |
| ASH-HERMES-* (1 entry) | — | `partially_mitigated` | `partially_mitigated` | **OPEN** — pre-existing, not re-triaged this run (bot filter excludes it too) |
| gateway Python deps | Unknown | n/a | n/a | **OPEN / NOT SCANNED** — see §7d |
| browser-extension npm deps | — | 0/0/0/0 | 0/0/0/0 | **CLEAN** |
| Untracked host services (LibreChat, searxng) | — | no pin | no pin | **OPEN / OUT OF SCOPE** — see §7g |
| Floating-tag containers (github-mcp-server, home-assistant) | — | `:latest`/`:stable` | unchanged | **OPEN / NEW FINDING** — see §7g |

## 9. Upgrade table

| Component | Env | Before | After | Status | Notes |
|---|---|---|---|---|---|
| OpenClaw | prod | `2026.9.4` | *(candidate: `2026.9.5`)* | **BLOCKED-awaiting-dev** | dev handoff missing/FAIL for today |
| Hermes | prod | digest `sha256:68e15ae2...` (`0.20.1`) | *(candidate: `v2026.9.14`)* | **BLOCKED-awaiting-dev** | same gap as 2026-09-17; unresolved A2A-probe history |
| AgentShroud (self) | prod | `1.7.0` | `1.7.0` | **ALREADY CURRENT** | verified via `docker/versions.env` + running image tags |
| Wazuh manager | prod | `4.14.7` | not checked against registry | **SKIPPED** | pinned already; registry re-check not performed within budget |
| Wazuh/Falco/ClamAV/Fluent Bit (in-process in gateway) | prod | — | — | **NOT INDEPENDENTLY VERSION-CHECKED** | ship inside gateway image build; currency tied to gateway image (current) |
| docker-socket-proxy | prod | digest-pinned | not checked | **SKIPPED** | see §7f |
| Base images (golang/node/python) | prod (build-time) | digest-pinned | not checked | **SKIPPED** | see §7f |
| CVE-triage pipeline scope (Cosign/Falco/Syft/Trivy/Wazuh) | — | 98 `under_review` | 98 `under_review` | **OPEN GAP — new finding** | see §7c; requires a code change to `triage-cve-mitigations.py`, belongs on dev |
| LibreChat datastores, searxng | prod (untracked) | running, unpinned in-repo | unchanged | **OUT OF SCOPE / GAP** | see §7g |
| github-mcp-server, home-assistant containers | prod (untracked) | `:latest`/`:stable` | unchanged | **OUT OF SCOPE / GAP — new finding** | see §7g |

## 10. Jira tracking

```
$ jira_search(jql="project = SCRUM ORDER BY created DESC")
{"total": -1, "issues": []}
$ jira_get_all_projects()
[]
```
Same broken state as 2026-09-17 (authenticates, returns zero projects/issues
for a query that should return many). **No SCRUM ticket was created or
commented on this run** — per the mission's rule, stating this explicitly
rather than silently skipping. This connector needs a human to check
cloudId/site mapping; it has now been broken across at least two consecutive
tracked runs.

## 11. Verification evidence

No `apply`/`verify` phase ran — nothing was applied, so there is no
post-upgrade health check. Prod state identical at start and end of this run
(§6). `curl -s localhost:8080/status` not re-checked separately this run;
`docker ps` health column (§6) is the evidence used.

## 12. Breaking changes / manual follow-ups

1. **Root blocker, now with a concrete lead: the `agentshroud-bot` dev
   session (`claude --resume agentshroud-dev`, PID 13333) has been running
   since 2026-09-19 14:28 EDT (~17h) without producing a result file.**
   Recommend the operator attach to the `asb` tmux session on the
   `agentshroud-bot` account and either let it complete or kill/restart it.
   This is the actual next action — everything else here is secondary.
2. **NEW: 98 security-advisory registry entries (Cosign×10, Falco×8, Syft×2,
   Trivy×5, Wazuh×73) are `under_review` and structurally unreachable by the
   automated triage script**, which only processes `bot == 'openclaw'`.
   Extend `scripts/triage-cve-mitigations.py`'s scope (or run it per-bot) —
   a real code change, route through dev like any other. One entry
   (Trivy CRITICAL CVE-2026-33634) is already moot in practice (fixed in
   0.69.3, running 0.74.0) but the registry doesn't reflect that.
3. **NEW: two host containers run on floating tags** —
   `ghcr.io/github/github-mcp-server:latest` (×4) and
   `ghcr.io/home-assistant/home-assistant:stable` — violating the
   never-unpinned rule, and neither has an owning compose file in this repo.
   Needs ownership assigned before it can be pinned.
4. **Hermes remains the priority once dev passes**: real newer image exists
   (`v2026.9.14`), largest HIGH count of the three images (386), documented
   history of failing its A2A compatibility probe.
5. **`gateway/` has no scannable Python dependency lockfile** —
   `pip-audit`/`pip_audit` unavailable this run; unresolved from 2026-09-17.
6. **`atlassian-agentshroud` Jira connector still returns zero
   projects/issues** despite authenticating — unresolved from 2026-09-17,
   now confirmed on a second consecutive run.
7. **`docs/security/cve-triage-gaps.md`'s summary table is last-run-only,
   not cumulative** — a run that finds nothing new to triage silently
   regresses the visible "Total triaged" number (75 → 0 today, reverted
   uncommitted rather than shipped). Worth fixing the script.
8. LibreChat datastores and searxng remain untracked in this repo (carried
   over from 2026-09-17, unchanged).
9. Base images and docker-socket-proxy were inventoried but not
   re-checked against their registries this run (carried over, unchanged).

## 13. Rollback instructions

**Nothing was applied to prod; there is nothing to roll back.** The only
filesystem change this run makes is this report itself
(`reports/upgrade-2026-09-20.md`), committed on branch
`chore/upgrade-2026-09-20`, not pushed, not merged. One transient edit
(`docs/security/cve-triage-gaps.md`, from the triage-script inspection in
§7c) was reverted with `git checkout --` before commit and never landed.

## 14. Time spent

Started 07:04 EDT, report written ~08:01 EDT — **~57 minutes**, inside the
90-minute budget. Ending here rather than continuing to poll for a dev
result: dev's cadence is outside this session's control, and §3b gives a
concrete, actionable lead (the stuck `agentshroud-bot` session) rather than
an unexplained absence.
