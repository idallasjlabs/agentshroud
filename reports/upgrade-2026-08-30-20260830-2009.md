# AgentShroud Weekly Upgrade Report — 2026-08-30

## Run history

This job ran twice today under the same tracked report file:

1. **First pass (07:34–08:26 ET), ABORTED** — Preflight found a dirty working tree
   (33,342 modified paths, 53 of them substantive non-graphify source/config edits) and, mid-run,
   detected the checked-out branch changing under it with no action from that session — signs of a
   concurrent process on the same working directory. Correctly stopped without touching anything.
   Full detail preserved below in "Appendix: first-pass abort evidence."
2. **Second pass (09:53–16:56 ET, this report), RESUMED** — Owner merged PR #425
   (`fix(ops): preflight tolerates graphify churn + pre-existing local edits`), amending
   `prompts/sunday-upgrade.md`: `graphify-out/**` never counts as dirty (hook-regenerated),
   other pre-existing uncommitted edits are recorded and left untouched (not stashed/reverted),
   and this job's own commits (there were none this run) must be pathspec-scoped. The amendment
   also formalized the dev/prod cross-account handoff contract that was already implied by the
   morning's work. This report documents the resumed pass.

## 1. Summary

**PARTIAL (all promotable prod components BLOCKED-awaiting-dev, finalized at the operator's
19:30 ET deadline; 2 components already resolved this morning by an earlier pass; 4 components
handed off to a sibling repo; stack healthy throughout, zero changes made by this session)**

No files were modified, staged, committed, or pushed by this session. No containers were
restarted, rebuilt, or repinned. Every running service is healthy and unchanged from before this
pass started, confirmed again at final close-out (see Verification evidence).

Finalized without dev clearance: `/Users/Shared/agentshroud-sunday/dev-result-2026-08-30.json`
never arrived. Full timeline in §6 item 1. Per the cross-account contract, that means **no
component below was promoted to prod this run** — every one blocked purely on the missing dev
signal is recorded as BLOCKED-awaiting-dev rather than left ambiguous.

## 2. Environment mapping

Discovered this pass, superseding the "not determined" placeholder from the aborted first pass:

- **Host**: marvin.local, account `ijefferson.admin` — this is the **prod** account. Per
  `prompts/sunday-upgrade.md`'s "Cross-account reality (marvin)" section, dev runs as a
  **separate session** (`agentshroud-dev`, contacted via cross-session message this run — see
  §6) on its own Colima VM; this session cannot start/stop/exec into it directly.
- **Live compose project**: `docker-compose -f docker/docker-compose.yml -p agentshroud`
  (the `docker compose` plugin form is broken on this host per `scripts/update-agentshroud.sh`
  comments — script uses the hyphenated `docker-compose` binary). Running under it:
  `agentshroud-gateway`, `agentshroud-openclaw`, `agentshroud-docker-socket-proxy`,
  `agentshroud-voice-gateway`.
- **Hermes** (`agentshroud-hermes-v2`, image `agentshroud/hermes:latest`) runs **outside** that
  compose project via `docker/bots/hermes/run-standalone.sh` — a separate lifecycle, separate
  rollback path (`versions.env.bak` + image digest, not `docker-compose down/up`).
- **Other compose files, not applicable to this host**: `docker/docker-compose.agentshroud-bot.
  {marvin,trillian,raspberrypi}.yml` are dev/lab-host overlays for the `agentshroud-bot` account
  and other physical hosts, not this account. `docker-compose.sidecar.yml` and
  `docker-compose.secure.yml` (repo root) are legacy example configs — not referenced by any
  live deploy script, and their `wazuh`/sidecar service definitions are **not what's actually
  running** (see §4, sidecar gap finding). `examples/docker-compose.{minimal,production}.yml`
  are documentation examples only.
- **searxng-local, librechat-mongodb, librechat-meilisearch, and the LibreChat app itself**
  (pm2-managed, not containerized) belong to a **sibling repo**, `~/Development/local-llms` —
  confirmed via `git remote -v` there (`idallasj/local-llms`) and its own `bootstrap-librechat.sh`
  /`teardown-librechat.sh` lifecycle scripts. Hermes has a real, confirmed runtime dependency on
  searxng-local (`docker/bots/hermes/run-standalone.sh:194-224`, `web_search` tool backend); no
  direct AgentShroud-side wiring to mongo/meilisearch was found (not on the shared
  `agentshroud_agentshroud-isolated` docker network, no env var references anywhere in this repo).
  LibreChat is a real, actively-used deployment (33 Mongo collections, real conversation data),
  not an orphaned/stale service as first suspected — it's just not compose-managed from this repo.
  Per owner direction mid-run, upgrading this stack was handed off to the `local-llms` peer
  session rather than actioned from here (out of this job's repo scope) — see §6.

## 3. Upgrade table

| Component | Env | Before | After | Status | Notes |
|---|---|---|---|---|---|
| agentshroud-gateway | prod | 1.6.0 | 1.6.0 | ALREADY CURRENT | Product's own version; no external vendor pull |
| agentshroud-openclaw | prod | 1.5.3 (implied) | **2026.7.1-2** | ALREADY CURRENT | Applied by an earlier pass today (commit `671de9cfa5`), compat-gated + 10/10 post-deploy checks, backup at `backup-update-20260830-073725/`. Not re-touched this run. |
| agentshroud-hermes-v2 | prod | digest `sha256:68e15ae2...` (v0.20.1) | unchanged | **BLOCKED** | Candidate `sha256:20c5941...` (v0.20.5) fails its A2A JSON-RPC probe in the candidate container, per this morning's pass. Not retried per owner instruction. |
| docker-socket-proxy | prod | `tecnativa/docker-socket-proxy:latest` (digest = tag `v0.5.0`) | unchanged | BLOCKED-awaiting-dev | Content current, but pinned to a **floating** tag — violates the "never leave unpinned" rule; needs an explicit pin to `v0.5.0`, gated on dev PASS like everything else below |
| agentshroud-voice-gateway | prod | locally built (python:3.12-slim base) | unchanged | BLOCKED-awaiting-dev | Newer digest available for the same floating base-image tag |
| `gateway/Dockerfile` base images (`python:3.13-slim`, `golang:1.26-trixie`) | prod | pinned digests | unchanged | BLOCKED-awaiting-dev | Newer digests exist for the same tags; no CVE diff performed |
| `docker/bots/openclaw/Dockerfile`, `docker/Dockerfile.agentshroud` base (`node:22-bookworm-slim`) | prod | pinned digest | unchanged | BLOCKED-awaiting-dev | Both files pin the identical digest — unclear if both are still built; flagged for human check before bumping twice |
| `containers/agentshroud/Dockerfile`, `containers/gateway/Dockerfile` | — | unpinned, no digest | unchanged | SKIPPED | Zero references found in any script/compose/CI this pass — likely dead duplicates; not touched without confirming they're truly unused |
| gateway: `fastapi`, `pydantic`, `cryptography`, `setuptools`, `presidio-analyzer` | prod | at floor | at floor | ALREADY CURRENT | Floor pins already equal latest stable |
| gateway: `presidio-anonymizer` | prod | 2.2.362 | unchanged | BLOCKED-awaiting-dev | 2 patch versions behind latest (2.2.364); floor satisfied but not latest |
| gateway: `websockets` | prod | 16.1.1 (ceiling `<17.0`) | unchanged | SKIPPED | Deliberate ceiling in `requirements.txt` — needs a changelog read before raising, not a mechanical bump |
| gateway: `uvicorn`, `aiohttp`; `voice_gateway/requirements.txt` (faster-whisper, numpy, kokoro, etc.) | prod | floor-pinned | unchanged | SKIPPED | Not individually version-checked this pass (time budget) — flag for next run |
| `chatbot/requirements.txt` (`openai` etc.) | — | floor `>=1.12.0` | unchanged | SKIPPED | `chatbot/` is not referenced in `docker/docker-compose.yml` — not deployed in prod |
| `browser-extension/package.json` (`jest`) | — | `^29.7.0` | unchanged | SKIPPED | Dev-only test dependency, no lockfile committed, not shipped |
| MCP: `xmind-generator-mcp`, `safari-mcp` | prod (`.mcp.json`) | unpinned (`npx -y`) | unchanged | **BLOCKED** | Floating-by-design; pinning requires changing `.mcp.json`'s invocation style — a tooling/policy decision, not applied without owner sign-off |
| MCP: `awslabs.aws-api-mcp-server` | prod (`.mcp.json`) | unpinned (`uvx ...@latest`) | unchanged | **BLOCKED** | Same, plus the server is itself upstream-deprecated (vendor recommends migrating to "AWS MCP Server") — a migration decision, not a version bump |
| Git submodule `firmware/voice-terminal/.../lvgl_kawaii_face` | — | `ef9b47c` (4 ahead of `v1.0.0`) | unchanged | SKIPPED | ESP32 firmware, outside the gateway security surface; needs a human tagging decision |
| Host: Colima, Docker, docker-compose (standalone) | — | 0.10.3 / client v29.7.2, server v29.5.2 / 5.5.0 | unchanged | SKIPPED (by policy) | Host tooling not owned by this job. **Flagging anyway**: Docker client/server version mismatch on this host is worth a manual look. |
| searxng-local, librechat-mongodb, librechat-meilisearch, LibreChat app | — (sibling repo `local-llms`) | searxng `:latest`@`sha256:37c616a...`; mongo:7 (7.0.34); meilisearch `:latest`; LibreChat via pm2 | — | **HANDED OFF** | Owner directed this be handled by the `local-llms` peer session, not this job (different repo, out of scope). Request sent this run (backup-first, pin-to-stable, report back); reply not yet received. `local-llms` was also asked to take ownership of its own recurring Sunday maintenance, including a model-freshness check, going forward. |
| Falco / ClamAV / Wazuh-agent / Fluent Bit sidecars | prod | **not deployed** | unchanged | **BLOCKED — see §4** | Not a version-bump question: these containers don't exist on this host at all, despite `docker/docker-compose.yml` mounting `wazuh-alerts`/`clamav-db` volumes into the gateway as if they did. This is a policy/architecture gap, not something this job can silently "upgrade" into existing. |

## 4. Security findings

Full sweep run against all 8 running prod containers (`trivy image`), plus GitHub
Dependabot/code-scanning/secret-scanning (`gh api`), the gateway's own SOC API
(`/soc/v1/security/*`), and the project's existing `docs/security/cve-triage-gaps.md`.

| ID | Severity | Component | Current | Fixed-in | Status |
|---|---|---|---|---|---|
| CVE-2026-59873 | CRITICAL | `tar` (openclaw, hermes) | 7.5.11 | 7.5.19 | OPEN — fix available, blocked on dev gate like everything else |
| CVE-2026-33747 | CRITICAL | `docker-cli` (hermes) | 26.1.5+dfsg1-9+b13 | +deb13u1 | OPEN — fix available |
| CVE-2025-68121 | CRITICAL | Go stdlib (mongo:7, sibling repo) | v1.24.6 | 1.24.13+ | OPEN — handed off with the rest of the mongo container to `local-llms` |
| CVE-2026-13221 / -42496 / -8376 | CRITICAL | perl/perl-base (gateway, openclaw, hermes, voice-gateway) | various | **none upstream** | ACCEPTED — no patched Debian build exists yet |
| CVE-2026-60002 | CRITICAL | openssh-client (gateway, openclaw, hermes) | various | none upstream | ACCEPTED — no fix released |
| CVE-2026-6653 | CRITICAL | libxml2 (gateway, openclaw, hermes, voice-gateway) | various | none upstream | ACCEPTED — no fix released |
| CVE-2023-45853 | CRITICAL | zlib1g (openclaw) | 1:1.2.13.dfsg-1 | none upstream | ACCEPTED — no fix released |
| CVE-2025-7458 | CRITICAL | libsqlite3-0 (openclaw) | 3.40.1-2+deb12u2 | none upstream | ACCEPTED — no fix released |
| CVE-2026-58016 | CRITICAL | libglib2.0 (openclaw, hermes, voice-gateway) | various | none upstream | ACCEPTED — no fix released |
| CVE-2026-34873 / -34875 | CRITICAL | libmbedcrypto16 (hermes, voice-gateway) | 3.6.5-0.1~deb13u1 | none upstream | ACCEPTED — no fix released |
| CVE-2026-43185 | CRITICAL | linux-libc-dev (hermes) | 6.12.100-1 | none upstream | ACCEPTED — no fix released |
| CVE-2026-14456 | HIGH | libcrypto3/libssl3 (docker-socket-proxy, meilisearch) | 3.5.6/3.5.7-r0 | 3.5.8-r0 | OPEN — fix available |
| CVE-2026-45447 | HIGH | libcrypto3/libssl3 (meilisearch) | 3.5.6-r0 | 3.5.7-r0 | OPEN — fix available (`local-llms` scope) |
| CVE-2026-33630 | HIGH | c-ares (meilisearch) | 1.34.6-r0 | 1.34.8-r0 | OPEN — fix available (`local-llms` scope) |
| CVE-2026-41066 | HIGH | lxml (searxng) | 6.0.4 | 6.1.0 | OPEN — fix available (`local-llms` scope) |
| CVE-2026-42544 | HIGH | granian (searxng) | 2.7.3 | 2.7.4 | OPEN — fix available (`local-llms` scope) |
| (aggregate) | HIGH/MED | gateway:1.6.0 / openclaw:1.6.0 / hermes:latest / voice-gateway | — | — | OPEN — 207H/144M, 214H/314M, 422H/1307M, 146H/250M respectively; full detail in trivy JSON, not committed (read-only run) |
| SOC-EGRESS-001 | HIGH (operational, not CVE) | Hermes → api.feedbin.com | 12 denied/timeout egress events, 2026-08-29 | — | OPEN — needs human review, not an upgrade item |
| **SIDECAR-GAP-001** | **HIGH (governance)** | Falco / ClamAV / Wazuh-agent / Fluent Bit | **not deployed on this host** | — | **OPEN — see below** |
| SCANNER-GAP-001 | Informational | Gateway's own scanner-integration cache | `clamav`, `trivy` (generic), `trivy:fs:/app` all report `status:"error"`, 0 findings | — | OPEN — the daily in-container CVE scan is itself silently broken |

**Flagging SIDECAR-GAP-001 prominently**: this repo's own `CLAUDE.md` states as a hard constraint
that Falco, ClamAV, Wazuh-agent, and Fluent Bit must not be "removed or stubbed" — but on this
prod host, **none of them are running, and none are even present as stopped containers**.
`docker/docker-compose.yml` mounts `wazuh-alerts`/`clamav-db` volumes into the gateway as if
these scanners populate them, but nothing does. This is not something a Sunday upgrade job can
fix by bumping a version — it's a deployment gap that needs an owner decision (deploy the
sidecars, or formally revise the governance doc). Recording it here rather than silently letting
scanner findings continue reporting `status:"error"` week over week.

**Coverage gaps (tooling failed, not skipped by choice)**: `pip-audit` failed identically on 4
attempts (its own ephemeral-venv bootstrap step errors; the same interpreter works fine outside
pip-audit's wrapper) — zero Python CVE coverage for gateway/chatbot/voice_gateway this run.
`npm audit` requires a lockfile that doesn't exist for `browser-extension/` (`ENOLOCK`) —
generating one would be a file write, out of scope for a read-only scan. `cargo audit` isn't
installed for `cli/`'s `Cargo.toml` and installing a new scanner is out of scope. GitHub
Dependabot alerts: 0 open. Code-scanning alerts: 0 open (all dismissed/fixed). Secret scanning:
disabled on this repo (relies on local gitleaks pre-commit instead).

## 5. Verification evidence

```
$ curl -sS http://127.0.0.1:8080/status
{"status":"healthy","version":"1.6.0"}

$ docker-compose -f docker/docker-compose.yml -p agentshroud ps
agentshroud-docker-socket-proxy   Up 31 hours
agentshroud-gateway                Up 9 hours (healthy)
agentshroud-openclaw                Up 9 hours (healthy)
agentshroud-voice-gateway           Up 31 hours (healthy)

$ docker ps --format '{{.Names}}\t{{.Status}}' | grep agentshroud
agentshroud-hermes-v2               Up 10 hours (healthy)

$ git status --short -- . ':!graphify-out' | wc -l
54   # identical count at start and end of this pass — confirms zero repo changes made
```

Dev-result gate:
```
$ ls /Users/Shared/agentshroud-sunday/dev-result-2026-08-30.json
No such file or directory
# polled up to 10 minutes at 09:53 ET; never staged.
# Final check at operator's 19:30 ET close-out deadline:
$ ls -la /Users/Shared/agentshroud-sunday/
total 0
drwxrwxrwx@   2 ijefferson.admin  wheel    64 Aug 30 07:57 .
drwxrwxrwt  112 root              wheel  3584 Aug 30 07:57 ..
# still empty — never staged
```

Final close-out health check (19:30 ET, immediately before finalizing this report):
```
$ curl -sS http://127.0.0.1:8080/status
{"status":"healthy","version":"1.6.0"}

$ docker ps --format '{{.Names}}\t{{.Status}}' | grep agentshroud
agentshroud-openclaw               Up 12 hours (healthy)
agentshroud-gateway                Up 12 hours (healthy)
agentshroud-hermes-v2              Up 13 hours (healthy)
agentshroud-voice-gateway          Up 33 hours (healthy)
agentshroud-docker-socket-proxy    Up 34 hours
```
All 5 core prod containers healthy/running; identical to every earlier check this run. Stack
left exactly as found.

Cross-session coordination (both replies pending as of report time):
- Messaged `agentshroud-dev` asking for root cause on why the handoff file was never staged.
- Messaged `local-llms` (twice): once to request a backup-first upgrade of
  searxng/LibreChat/mongo/meilisearch, once to hand it ownership of its own recurring Sunday
  maintenance including model-freshness checks.

Trivy/gh/SOC scan evidence: full per-image JSON in the security-sweep subagent's scratchpad
(not committed to the repo — this was a read-only pass); severity counts and CVE IDs above are
exact from that output. Two historical scans (`reports/security/trivy/trivy-*-20260629*.json`,
`...-20260713*.json`) were used as a sanity check and show comparable order-of-magnitude
CRITICAL/HIGH counts, confirming today's numbers aren't a scan artifact.

## 6. Breaking changes / manual follow-ups

For the owner:

1. **Dev-result handoff never fired — root cause confirmed by the `agentshroud-dev` peer
   session, full timeline below.** Dev's cron entry was only installed *today* (2026-08-30 —
   first at 06:00, moved to 03:00 by PR #424), after both windows had already passed; there was
   no scheduled dev run today to produce the file. First real scheduled run is next Sunday,
   2026-09-06 03:00 ET.

   - The dev session then began completing today's work **manually**: disk prune, voice-gateway
     rebuilt and confirmed healthy (`HEAD /health` fix deployed), then started an OpenClaw
     2026.7.1-2 parity build via `update-agentshroud.sh` (compat gate + backups + post-deploy
     gate) to match prod's already-applied version.
   - ~17:30 ET: dev reported the OpenClaw compat build in progress with a within-the-hour ETA
     and a commitment to stage a PASS-or-FAIL result file either way, then went silent — no
     further message received.
   - At the operator's 19:30 ET deadline for closing out today's run,
     `/Users/Shared/agentshroud-sunday/` is still empty (confirmed by `ls`, final check).
   - **Result: this run is finalized without dev clearance.** Every component whose only blocker
     is the missing dev signal is recorded as BLOCKED-awaiting-dev below, not promoted, not
     guessed at. **Net effect: prod stayed exactly where it was all day, which is the safe
     outcome the contract is designed to produce even when dev never finishes.**

   **Known issue for next Sunday's dev run** (found by the dev session while diagnosing its own
   compat build): `scripts/check-vendor-compat.sh` writes its build logs to hardcoded paths —
   `/tmp/check-vendor-compat-openclaw-build.log` (line 94), `/tmp/check-vendor-compat-hermes-
   build.log` (line 187), and a hardcoded `/tmp/check-vendor-compat-hermes-optdata-$$` working
   dir (line 241) — rather than an account-scoped temp location. Confirmed present in the script
   at those line numbers. Since dev and prod run under separate macOS accounts on the same host,
   this can produce cross-account `EACCES` false-failures if a file at one of those fixed paths
   already exists with the other account's ownership from a prior run. Worth fixing
   (`mktemp`/account-scoped `TMPDIR`) before next Sunday's dev run relies on this script again.
2. **local-llms handoff pending.** Sent two requests to the `local-llms` peer session (upgrade
   searxng/LibreChat/mongo/meilisearch with a backup-first plan given real user data in the
   `librechat` Mongo database; take ownership of its own recurring Sunday maintenance including
   model-freshness checks). Neither reply had arrived as of this report. Follow up with that
   session directly, or re-run this job later to pick up the outcome.
3. **SIDECAR-GAP-001** (above) needs an owner decision: deploy Falco/ClamAV/Wazuh-agent/Fluent
   Bit on this prod host, or revise `CLAUDE.md`'s hard constraint to reflect that they're
   currently not part of the deployed stack.
4. **SCANNER-GAP-001**: the gateway's own daily in-container CVE scan (`clamav`, `trivy` generic,
   `trivy:fs:/app`) has been silently returning `status:"error"` with 0 findings — worth a human
   look independent of this job, since it means the daily automated scan isn't actually catching
   anything right now.
5. **MCP server pinning** (`xmind-generator-mcp`, `safari-mcp`, `awslabs.aws-api-mcp-server`) is
   BLOCKED on a policy decision: switching `.mcp.json` from `npx -y`/`uvx ...@latest` to explicit
   pins is a tooling-behavior change, not a mechanical version bump — did not apply without
   sign-off, per the "do not modify security policy to make an upgrade pass" rule's spirit.
   Separately, `awslabs.aws-api-mcp-server` itself is upstream-deprecated; a pin doesn't solve
   that, only a migration to the vendor's recommended replacement does.
6. **docker-socket-proxy** and four Dockerfile base images are on floating tags with newer
   digests available for the same tag — flagged as BLOCKED-awaiting-dev rather than applied,
   since "apply nothing to prod" was in effect for the whole run regardless of how small the
   change looks.
7. **pip-audit/npm audit/cargo audit coverage gaps** (§4) mean this run has **no verified
   Python/JS/Rust dependency-CVE coverage** — trivy's OS-package scan doesn't substitute for
   that. Worth fixing the pip-audit venv-bootstrap failure before next Sunday rather than
   carrying the gap forward again.
8. **Two Dockerfiles** (`containers/agentshroud/Dockerfile`, `containers/gateway/Dockerfile`)
   look like dead duplicates (no script/compose/CI reference found this pass) but weren't
   confirmed dead with certainty — grep didn't cover `.github/workflows/` or Makefiles.

## 7. Rollback instructions

Nothing to roll back from **this** session — zero files, containers, images, or dependencies were
touched. For completeness, the rollback path for the one component an **earlier** pass today did
change (OpenClaw → 2026.7.1-2) remains, unused and untouched by this session:

```
cp backup-update-20260830-073725/versions.env.bak docker/versions.env
bash scripts/restore-backup.sh backup-update-20260830-073725
```

Repo-level baseline (also unused, HEAD unchanged since PR #425 merged):
```
git rev-parse HEAD   # 4b3b558215907c9c66bb703793cb3e4a0047f960
git describe --tags  # pre-deploy-20260830T112852Z-5-g4b3b558215
```

## 8. Time spent

First pass: 07:34–08:26 ET (aborted at Preflight). Resumed pass: 09:53 ET through finalization
at the operator's 19:30 ET deadline (~9.6 hours wall-clock for the resumed pass alone), well
past the prompt's 90-minute work budget. The overwhelming majority of that was waiting on two
things outside this session's own work: a single background security-scan subagent whose
`pip-audit` retries and 8-image `trivy` sweep ran ~6h14m real time, and then the dev-side
handoff (dev's cron didn't exist until today, so dev worked its Sunday task manually and never
finished staging a result in time). Per the budget rule ("if past 90 minutes and not finished,
stop, ensure both stacks are healthy, report what remains"): both stacks were healthy
throughout and remain so at close-out, nothing was left half-applied, and this report captures
everything that remains (§6). Finalizing now at the operator's explicit instruction rather than
continuing to wait on dev.

---

## Appendix: first-pass abort evidence (07:34–08:26 ET, superseded)

Preserved for the record. **Summary: ABORTED (unsafe preflight — dirty tree + concurrent
session).** At session start: `git status --short | wc -l` = 33,342; 53 non-graphify files
substantively modified (`gateway/security/{a2a_governance,drift_detector,health_report,
network_validator,token_validation,trust_manager}.py`, 10 files under `gateway/tests/`,
`gateway/tools/agentshroud_manager.py`, `gateway/web/management.py`, hook/config files, 28
files under `docker/config/{hermes,openclaw}/cron/prompts/`). Mid-run, the checked-out branch
changed from `fix/sunday-upgrade-submit` to `main` and HEAD advanced by two merged PRs (#423,
#424) with no action from that session — evidence of a concurrent live session (the same
Sunday-upgrade automation, run as a phone-visible Remote Control session) actively operating on
the same working directory. The run stopped rather than risk a collision. No stash was created
or popped; no files were modified. This was resolved by owner action (PR #425, see "Run
history" above) rather than anything this job could self-correct.
