/* Host-native stub: replaces audio.h for wakeword PTT state tests.
 * Only the constants used by wakeword.c are needed. */
#pragma once
#define AUDIO_SAMPLE_RATE      16000
#define AUDIO_FRAME_BYTES_MAX  1024
