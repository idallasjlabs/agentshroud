# =============================================================================
# Atlassian MCP — agentshroud tenant (agentshroudai.atlassian.net)
# =============================================================================
# Copy this file to .env and fill in JIRA_TOKEN below.
# The real .env is gitignored; this example file is committed.
#
# Credential resolution order in mcp-atlassian.sh:
#   1. JIRA_TOKEN env var already exported in the caller's shell
#   2. This .env file (sourced via `set -a`) — use this on Linux hosts
#   3. 1Password via `op read 'op://Agent Shroud Bot Credentials/AgentShroud - Google/atlassian api token'`
#
# Linux / no-1Password setup:
#   cp .env.example .env
#   # Generate a Jira API token at: https://id.atlassian.com/manage-profile/security/api-tokens
#   # Account: agentshroud.ai@gmail.com
#   nano .env   # fill in JIRA_TOKEN below, then save
# =============================================================================

JIRA_TOKEN=
