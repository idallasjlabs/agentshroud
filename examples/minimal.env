# OneClaw — Minimal Configuration
# Bare minimum to get the gateway running

# Required: Authentication token (generate with: python3 -c "import secrets; print(secrets.token_hex(32))")
GATEWAY_AUTH_TOKEN=CHANGE_ME

# Gateway settings
GATEWAY_BIND=127.0.0.1
GATEWAY_PORT=8080

# Logging
LOG_LEVEL=INFO
