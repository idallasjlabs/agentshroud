# Real deployment config (contains personal IPs, phone numbers, hostnames)
../agentshroud.yaml

# Secrets - NEVER commit
secrets/*.txt
secrets/*.key
secrets/*.pem

# Extensionless secret files
secrets/1password_service_account

# Exclude README (documentation is OK)
!secrets/README.md
