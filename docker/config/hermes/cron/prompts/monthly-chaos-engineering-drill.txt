First of month chaos engineering drill (Hermes). Simulate one failure scenario for AgentShroud involving Hermes: gateway crash, volume corruption, bot disconnect, or dependency outage. Describe failure mode, detection method, blast radius, and recovery procedure. Use *bold* for key terms/numbers and short scannable lines (blank line between sections is fine) — no markdown headers (#) or tables, Telegram does not render them.