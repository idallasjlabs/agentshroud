Check upstream Turbo Fieldfare issue https://github.com/drumih/turbo-fieldfare/issues/84 (the tool-call decoder_consume/malformed bug blocking Hermes on that backend) for a fix. Fetch ONLY https://api.github.com/repos/drumih/turbo-fieldfare/issues/84 (state + updated_at, single call, no auth needed, public repo) and https://api.github.com/repos/drumih/turbo-fieldfare/issues/84/comments (just check the last 1-2 entries for anything new from the maintainer — do NOT dump the full page). If the issue is now CLOSED, or the maintainer posted a new comment suggesting a fix/root-cause/PR since our last comment (https://github.com/drumih/turbo-fieldfare/issues/84#issuecomment-5284001871), lead with the fix summary and the PR/commit link if any. Otherwise reply with ONE short line: current state, still open or closed, and the date of the most recent comment. Keep it under 80 words either way — this should be a quick daily check, not a research report.