Health-check the oMLX local model backend's streaming reliability for whatever MoE (Mixture-of-Experts) model is currently configured (check /app/agentshroud.yaml or your own model config for the active local model, e.g. gemma-4-12B-it-4bit or gemma-4-26b-a4b-it if that's what's set). Send ONE test streaming chat completion request directly to the oMLX endpoint (http://host.docker.internal:8000/v1/chat/completions, Authorization: Bearer using OMLX_API_KEY if set, model set to the active model, a short prompt like 'Reply with one short sentence.', stream:true) via curl with a 90s timeout, and measure: time to first token, whether the stream completes without stalling, and total latency. Report a ONE-LINE status via Telegram: model name, time-to-first-token, total latency, and whether streaming completed normally or stalled/timed out. Keep it brief — this is a quick daily health probe, not a deep investigation. If the request fails outright (connection refused, timeout), report that plainly as a failure, not as 'no notable findings'.