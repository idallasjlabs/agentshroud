Nightly memory consolidation. Steps: 1) Determine today's date in YYYY-MM-DD format. 2) Read /home/node/.openclaw/workspace/memory/journal/[today's date].md. The journal/ directory already exists — do NOT attempt to create it, do NOT run mkdir. If the file does not exist or is empty, treat its content as 'No notable events today.' 3) Consolidate all journal entries from today into /home/node/.openclaw/workspace/memory/context.md — overwrite any previous content. This file must remain under 50 lines and capture: active projects and their current status, pending tasks and action items, recent decisions made by the owner, and key facts about the system or collaborators that any model should know to maintain continuity. Write in concise bullet-point form grouped under headings: ## Active Projects, ## Pending Tasks, ## Recent Decisions, ## Key Facts. Do not include conversation transcripts.

ZERO HALLUCINATIONS ON ERRORS: if any tool call fails, you must never state a cause you did not directly observe in that tool's own error output (e.g. never claim "read-only filesystem" or "directory does not exist" unless the tool's literal error text says exactly that — check first, most tool failures here are transient, not filesystem state). Retry once; if it still fails, quote the exact error text verbatim, do not paraphrase or guess at a plausible-sounding cause.

This is a silent background operation — the owner is never notified, success or failure. Your final reply must be exactly NO_REPLY and nothing else, in every case: after a successful consolidation, after a "no notable events" day, and even after a tool failure (log what actually happened by writing one line to context.md's Key Facts section instead of messaging the owner). NO_REPLY is OpenClaw's own suppression token — anything else you write as your final reply gets delivered to Telegram as a visible message.
