# 2026-03-22 — Session Notes

## Slack vs Telegram Security Parity Analysis (12:00 UTC)

Isaiah asked whether collaborators have the same enforcing modules in Slack as Telegram. After reviewing `slack_proxy.py` and `telegram_proxy.py`:

**Finding: Significant asymmetry.** Slack enforcement is outbound-only.

- **Same (shared SecurityPipeline):** Both channels use `process_outbound()` for outbound content — PII sanitization, outbound info filter, canary tripwire, encoding detection, egress filter, prompt protection, fabricated security notice detection.
- **Telegram-only (not in Slack):**
  - 30+ deterministic `_looks_like_*` pattern-matching inbound blockers
  - Progressive lockdown (3/5/10 threshold → session suspension)
  - Collaborator rate limiting, stranger rate limiting, access request queue
  - RBAC enforcement at proxy layer, collaborator command blocking
  - Full `process_inbound()` pipeline (PromptGuard, ContextGuard, HeuristicClassifier)
  - Middleware pipeline (RBAC, context guard, multi-turn tracking)
  - Collab mode routing (local_only / project_scoped / full_access)
  - Owner ID redaction, HTML markup stripping, outbound block cascade, over-length blocking
- **Slack inbound:** Handled natively by OpenClaw Socket Mode — bypasses gateway entirely.
- **Risk:** Collaborator blocked on Telegram could reach bot through Slack with same prompts.

## Dev Helper Script Fix (13:25-13:32 UTC)

- `~/bin` on marvin was a FILE (tmux launcher), not a directory. The `dev` helper never existed.
- Root cause of inability to run builds: SSH proxy blocks compound operators. Without wrapper script, docker compose workflows impossible remotely.
- Isaiah moved `~/bin` → `~/tmux-launcher`, created `~/bin/dev` with build/rebuild/up/down/pull/status/logs/test commands.
- Remote calls need full path: `ssh marvin "/Users/agentshroud-bot/bin/dev status"`
- Also installed on raspberrypi and trillian.

## Container Rebuild Attempt (13:28-13:51 UTC)

- Compose: `docker-compose.agentshroud-bot.marvin.yml` (dev overlay, project `agentshroud-dev`)
- Both images built successfully (gateway ~310s, bot ~393s)
- **Startup failed:** secrets files owned by `ijefferson.admin:staff` mode 640 — agentshroud-bot can't read them
- Fix needed: `chmod 644 ~/Development/agentshroud/docker/secrets/*.txt` (as admin user)
- Containers not yet running. Peer review / QA / security assessment blocked on this.

## Git State on Marvin

- Branch: feat/v0.9.0-soc-team-collab (HEAD at 299f9ac)
- Recent: Falco consolidation, cron path fix, self-journaling protocol, model mode default
- Clean working tree

## Secrets Symlink Root Cause (14:00 UTC)

- `docker/secrets` in git is a **symlink** → `/Users/ijefferson.admin/Development/agentshroud/docker/secrets`
- Colima VM only mounts `$HOME` (`/Users/agentshroud-bot`) by default — can't follow symlinks to `/Users/ijefferson.admin/`
- Docker error: "bind source path does not exist" (really: path unreachable inside VM)
- Fix: Isaiah ran `rm secrets && cp -r /Users/ijefferson.admin/.../secrets . && chmod 644 secrets/*.txt`
- **Problem recurs on every `git pull`** — git restores the symlink. Needs `.gitignore` or post-checkout hook.

## Dev Containers Running + Initial Assessment (14:00-16:14 UTC)

- After symlink fix, both containers started successfully
- Gateway: healthy @ 127.0.0.1:9080, all 30+ security modules initialized
- Bot: healthy @ 127.0.0.1:19789, all secrets loaded, Slack + Telegram configured
- **Telegram channel in death spiral** due to C1 (PROXY_ALLOWED_NETWORKS mismatch)

### Full Peer Review / QA / Security Assessment Delivered

**🔴 CRITICAL (2):**
- **C1:** `PROXY_ALLOWED_NETWORKS=172.21.0.0/16` only allows isolated network. Bot Telegram API calls come from 172.20.0.3 (internal). All Telegram calls → 403. Bot in restart loop (attempt 7/10). Fix: add `172.20.0.0/16`.
- **C2:** Slack inbound has ZERO security pipeline coverage. Socket Mode bypasses gateway.

**🟠 HIGH (4):**
- **H1:** `resource_guard.py` measures process-wide CPU/memory — per-agent limits meaningless
- **H2:** Bot Telegram restart loop exhausting retries
- **H3:** Duplicate `import asyncio` in `egress_filter.py _record()` on high-frequency path
- **H4:** `set_global_mode("monitor")` destroys custom thresholds on restore

**🟡 MEDIUM (5):** M1: telegram_proxy.py 6044 lines, M2: ClamAV scan-per-request, M3: 1Password uncached, M4: missing competitive-analysis.md, M5: GitHub 404s

**🟢 LOW (3):** L1: tmux/python3 missing in bot, L2: wildcard one-level only, L3: global singleton side effect

**✅ Positive:** Fail-closed pipeline, layered defense, AuditChain SHA-256, progressive lockdown, default-deny egress, SSRF protection, 201K DNS blocklist, per-collaborator isolation, container hardening

## Second Rebuild — 14 New Commits (19:51-20:15 UTC)

Isaiah requested rebuild from latest. 14 new commits since 299f9ac (HEAD now `7d80674`):
- `23d1ddb` fix: C1 dev death spiral + Activity tab audit overhaul
- `c5d85a5` feat: Slack Socket Mode WebSocket relay for inbound activity tracking
- `f42bfe2` feat: eliminate security theater — wire real enforcement
- `54e9eac` feat: SOC hardening — Slack security parity, egress history, logs fallback
- `9834efb` feat: fix all 44 SOC Command Center issues (CC-01 through CC-44)
- Plus: DEVELOPER.md injection, SESSION_PROMPT.md, npm fixes, bot self-upgrade endpoint

Both images rebuilt successfully (~10 min). **Startup failed again** — git pull restored secrets symlink. Same Colima VM mount issue. Awaiting Isaiah to re-run symlink→copy fix. Suggested permanent fix: `.gitignore` or post-checkout hook.
