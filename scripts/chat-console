#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

echo "Starting AgentShroud Chat Console..."
cd "${REPO_ROOT}"
exec python3 src/interfaces/chat_console.py
